# linquiz（小綸出題小幫手）進度

## 架構
- `public/index.html`：前端（純靜態頁，之後用 Cloudflare Pages 部署）
- `worker/`：後端 API（Cloudflare Worker，wrangler 部署，負責藏 Gemini API Key + 冷卻限制）

原本是單一 Worker 檔案同時回傳 HTML 又當 API Proxy，且 API Key 由使用者自己貼在前端。
這次改成前後端分離：Key 改放 Worker 端環境變數，前端完全看不到。

## 已完成
- 前端**保留**「使用者自填 API Key」欄位（2026-07-24 確認：先前紀錄寫「已移除」是誤記，實際 UI 一直都在，故意保留讓重度使用者不受冷卻限制）
- 後端 Worker：
  - Gemini API Key 只存在伺服器端環境變數，前端完全看不到、也無法自帶 Key
  - **支援多組 Key 輪換**：`env.GEMINI_API_KEYS`（逗號分隔多組 Key，secret，不進 git；只設一組時可用舊的 `env.GEMINI_API_KEY`）。每次請求隨機選一組起始 Key，該模型失敗就換下一組 Key，同一模型下所有 Key 都試完才換下一個模型
  - 冷卻機制：以來源 IP 為單位，透過 KV (`COOLDOWN_KV`) 記錄上次出題時間，30 秒內重複「開始出題」會回 429 + 剩餘秒數
  - 只在每次出題的第一個批次（`X-Batch-Index: 0`）檢查冷卻，避免題數多、批次多時卡到自己
  - 管理者密碼 `5407`（`X-Admin-Pass` header）可略過冷卻，密碼可用 `ADMIN_PASSWORD` secret 覆蓋預設值
- 前端新增冷卻倒數 UI（按鈕會顯示「⏳ 冷卻中，請等待 N 秒」並自動解鎖）
- 前端右上角「🔓 管理者解除限制」按鈕，輸入密碼後之後的請求會自動帶上 admin header
- 模型比照 `D:\ClaudeOnly\SteveProject\lunslip` 的作法，改成雙模型容錯：後端固定依序嘗試 `gemini-3.5-flash` → `gemini-3.1-flash-lite`，前者失敗（額度滿/暫時異常）就自動 fallback 到後者，不會整批直接失敗。原本寫死的 `gemini-3-flash-preview` 不是有效模型名稱，已移除。

## ✅ 後端已部署並驗證(2026-07-23)
- KV namespace `COOLDOWN_KV` 建立完成,id=`53865c1abe754d3faf61c3492b4060cd`,已寫入 `worker/wrangler.toml`
- Steve 貼了 7 組 Key(`AQ.Ab8...` 開頭,從 Google AI Studio 拿的),已用 `wrangler secret put GEMINI_API_KEYS` 設定(逗號分隔),原本以為格式異常,**實測確認合法有效**(跟 school/el 專案先前踩過的坑一樣,`AQ.Ab8...` 是合法格式)
- Worker 部署完成:`https://linquiz-api.steve-edu711.workers.dev`
- 用 curl 帶 `X-Admin-Pass: 5407` 直接測 `/api/generate`,回傳正確 JSON(用 `gemini-3.1-flash-lite`,`gemini-3.5-flash` fallback了但整體成功) — 確認 Key 池與雙模型容錯機制正常運作
- 前端 `public/index.html` 的 `API_BASE` 已更新為正式 Worker 網址
- 已建立新 repo `steveedu711-oss/lunquiz`(public),`git subtree push --prefix=school/lunquiz` 推送完成,分支 `master`

## ✅ Cloudflare Pages 已連結(2026-07-24,Steve手動在Dashboard完成Git連結)
- Cloudflare CLI/API 無法建立「Git連結自動部署」的 Pages 專案(只能建純手動上傳的專案,違背部署標準),這步驟已請 Steve 自行在 Dashboard 走 GitHub OAuth 連結流程完成
- **手機版UI適配**(2026-07-24新增):
  - `.a4-page`(試卷預覽)原本寫死 `width:210mm`(A4),手機螢幕會被撐開;新增 `@media screen and (max-width: 820px)` 與 `480px` 兩層,螢幕預覽時改為 `width:100%`、縮小 padding/字級,**只影響螢幕預覽,不影響 `@media print` 的實際列印/下載排版**(A4 print規則獨立未動)
  - 出題模式選擇的兩個大按鈕(AI智慧搜尋/上傳檔案)在窄螢幕改直式排列(`flex-col sm:flex-row`),避免小手機並排擠壓
  - 出題結果頁上方 sticky 工具列(題目卷/答案卷切換+提示文字+重新出題/下載/列印)加 `flex-wrap`+響應式字級/間距,窄螢幕會自動換行,提示文字換到第二行置中,不會橫向溢出
  - `body` 加 `overflow-x:hidden` 防止意外橫向捲動
  - 表單本身(年級/科目/版本/範圍/題數/題型/難度/語言)原本就用 Tailwind `md:grid-cols-*`,手機本來就會自動變單欄,這次沒有動

## ✅ 7組Key逐一驗證+真實瀏覽人次計數器(2026-07-24)
- 新增 `/api/keys-check`(管理者密碼保護,GET,呼叫 Gemini `models` list 輕量驗證,不耗生成額度,回應只顯示Key末4碼)——實測 7 組 Key **全部有效**
- 新增 `/api/visit`(GET,真實計數,非前端假數字):以 `CF-Connecting-IP + 當天日期` 在既有 `COOLDOWN_KV` 做去重(key: `visited:<ip>:<date>`,TTL 86400秒),同一人同一天重整頁面不會重複累加,總數存在 `visit_count` key。已實測連續呼叫3次確認去重正常(count 都停在1)
- 前端 `public/index.html` 最上面(banner圖片之前)加醒目的綠色圓角標籤「👣 累計造訪人次」,`window.onload` 時 fetch `/api/visit` 顯示
- Worker 已重新部署至 `https://linquiz-api.steve-edu711.workers.dev`,`CORS_HEADERS` 方法補上 `GET`
- KV `visit_count` 起始值已用 `wrangler kv key put ... --remote` 設為 **27677**(Steve指定的起始基準數字,非從0算起)

## ✅ 造訪人次UI改醒目位置+起始值調整(2026-07-24)
- 造訪人次標籤從banner圖片下方移到**整個頁面最上面**(banner圖片之前),改成綠色圓角徽章樣式更顯眼
- 起始基準數字改成 **27677**(用 `wrangler kv key put --namespace-id=53865c1abe754d3faf61c3492b4060cd visit_count 27677 --remote` 設定,注意預設不加 `--remote` 會寫到本機模擬KV,要記得加這個flag)
- 已 commit + `git subtree push --prefix=school/lunquiz lunquiz master`(ClaudeOnly根目錄新增了 `lunquiz` remote 指向 `steveedu711-oss/lunquiz.git`)發布,Cloudflare Pages會自動部署
- **推送時踩到帳號坑**:預設gh作用中帳號是`safezone0619`對這個repo沒權限(403),需先`gh auth switch --user steveedu711-oss`才推得動,推完記得`gh auth switch --user safezone0619`切回來(已切回)

## ✅ 冷卻機制改用裝置ID,解決同校網路互卡問題(2026-07-24)
- 前端 `getClientId()`：用 `localStorage.lun_client_id`(crypto.randomUUID)為每個瀏覽器產生固定裝置ID,出題請求帶 `X-Client-Id` header
- worker `handleGenerate` 冷卻key改成 `cooldown:${clientId || ip}`(裝置ID優先,舊前端沒帶ID才退回IP),CORS header補上 `X-Client-Id`
- 已用curl實測驗證：同一裝置ID第二次請求正確被429擋下,但同IP不同裝置ID的請求完全不受影響(不會互卡)
- Worker已重新部署,前端已 commit+push(`3f20ea3`)到 `steveedu711-oss/lunquiz`,Cloudflare Pages自動部署中

## ✅ 出題紀錄+複製設定連結+單題重新出(2026-07-24)
- **出題紀錄**:`saveQuizToHistory()` 每次成功出題後存進 `localStorage.lun_history`(最多留20筆,超過自動丟最舊的);「📜 出題紀錄」按鈕(表單右上角)開彈窗可「開啟」(帶回表單設定+重繪試卷)或「刪除」。**僅存在該台裝置瀏覽器**,換裝置看不到
- **複製設定連結**:「🔗 複製目前設定連結」把年級/科目/版本/範圍/題數/題型/難度/語言編碼進URL query string,複製到剪貼簿分享給同事;對方開連結時 `applySettingsFromURL()` 自動帶入表單(含自訂欄位會自動切到Custom並填入)
- **單題重新出**:每題左上角新增「🔁 重新出這題」按鈕(`.no-print`,不會出現在列印/Word匯出),點擊會針對該題重打一次 `/api/generate`(帶原本的年級/科目/版本/範圍/難度/語言+其餘題目清單要求不要重複),成功後只替換該題DOM(不影響其他題目的手動編輯內容)。**沿用同一套裝置ID冷卻機制,一樣受30秒限制**,冷卻中會跳429並觸發跟主出題按鈕一樣的冷卻倒數UI
- `dlDoc()`(Word匯出)已加上移除 `.no-print` 元素的邏輯,避免重出按鈕跑進匯出檔案
- 已用 Node `new Function()` 驗證內嵌JS語法正確;三項功能**都還沒有實機/瀏覽器實測**(受限於本機無法操作瀏覽器),邏輯是照既有程式碼風格手寫並肉眼審閱過
- 已 commit(`3615d0d`)+ subtree push 到 `steveedu711-oss/lunquiz`,Cloudflare Pages 自動部署中

## ✅ 下載多格式(Word/PDF/純文字)+手機線上作答自動評分(2026-07-24)
- **下載改成下拉選單**(點「⬇️ 下載 ▾」展開)：
  - 📄 Word (.doc)：沿用原本 `dlDoc()` 邏輯不變
  - 📕 PDF：新增 `dlPDF()`，用 CDN 引入的 `html2canvas`(1.4.1) + `jsPDF`(2.5.1 UMD) 把目前顯示中的每張 `.a4-page`(題目卷或答案卷，看當下切到哪頁)截圖成 canvas 再組成A4尺寸PDF；截圖前會暫時把 `.no-print`(重出題按鈕)設 `visibility:hidden` 避免混進圖片，截圖完再還原
  - 📝 純文字 (.txt)：新增 `dlTxt()`，抓目前顯示頁面的 `innerText` 直接輸出
  - 下拉選單用 `toggleDownloadMenu()`/`closeDownloadMenu()` 控制，並綁 `document.addEventListener('click', ...)` 點外部自動關閉
- **手機線上作答＋自動評分**：sticky header 新增「📱 線上作答」分頁（第三個分頁，跟題目卷/答案卷同排）
  - `renderAnswerMode()`：把 `window.currentItems` 轉成互動表單，選擇題用 radio、複選題(依 `q.a` 是否為多個字母/陣列判斷)用 checkbox，填空/簡答題用 textarea
  - `gradeAnswers()`：選擇/複選題精確比對字母組合自動判分；填空/簡答題只能做「去空白後字串完全相同」的粗略比對(標記僅供參考，會顯示參考答案+解析供老師自行核對)，不是AI語意評分
  - 送出後顯示總分(依各題 `q.s` 配分加總)與百分比，每題下方即時顯示✅/❌回饋
  - 切分頁邏輯(`switchSheet`)重構成支援三個分頁；`renderPaperFixed()`與 `regenerateQuestion()` 都會重置 `answerModeContainer.dataset.rendered`，確保重新出題/單題重出後再切到作答分頁會拿到最新題目
- 已用 Node `new Function()` 驗證語法；**這批功能同樣還沒有實機/瀏覽器實測**，尤其 PDF 匯出(html2canvas截圖中文字型/分頁裁切效果)跟手機作答UI的實際觸控體驗最需要肉眼確認
- 已 commit(`cbeafd0`)+ subtree push 到 `steveedu711-oss/lunquiz`，Cloudflare Pages 自動部署中

## ✅ 分享題目(學生作答)/分享答案(家長)兩個分享按鈕(2026-07-24)
- **worker新增**：
  - `POST /api/share`：把 `{items, title}` 存進既有 `COOLDOWN_KV`(key前綴 `share:`+12碼隨機ID)，30天後自動過期(`expirationTtl`)，單筆內容上限500KB(超過回400)，回傳 `{id}`
  - `GET /api/share/:id`：讀取，找不到回404("可能已過期或連結錯誤")
  - 已用curl實測：建立→讀取→讀不存在ID(404)都正確；部署後**第一次測試遇到短暫邊緣節點傳播延遲**(前幾秒回退到預設「linquiz API worker is running.」訊息)，等幾秒後重試就正常了——如果之後其他新端點也遇到「明明程式碼對但回應像沒路由到」，先懷疑是這個延遲，不用急著改程式碼
- **前端新增**：出題完成後 sticky header 多兩顆按鈕「📤 分享題目(學生作答)」「📤 分享答案(家長)」
  - `shareQuiz(mode)`：把 `window.currentItems` 存到後端拿到ID，組出 `?share=ID&mode=student|key` 連結複製到剪貼簿
  - `loadSharedQuizIfAny()`：頁面載入時偵測網址帶 `share` 參數，直接跳過表單、fetch題目、呼叫 `renderPaperFixed(items, title)`(新增第二參數titleOverride，優先用分享時存的標題而非表單欄位推導)，並移除所有 `contenteditable` 避免學生/家長誤改內容
  - `applySharedViewChrome()`：依 `mode` 隱藏老師專用按鈕——`mode=student` 隱藏題目卷/答案卷/線上作答三個分頁按鈕(只留作答表單，學生看不到解答)；`mode=key` 只隱藏線上作答分頁(家長仍可切題目卷/答案卷)；兩種模式都隱藏重新出題/下載/分享按鈕
  - **踩到的坑**：`switchSheet()`會整個覆寫按鈕`className`，所以`applySharedViewChrome()`(負責加`hidden` class)必須在`switchSheet()`**之後**呼叫，不然hidden會被洗掉——目前程式碼順序已修正為先switchSheet再applySharedViewChrome
  - `regenBtnHTML()`在`window.isSharedView`時直接回傳空字串，避免分享出去的頁面上還留著「🔁重新出這題」按鈕
- 已用 Node `new Function()` 驗證語法；worker端已用curl實測，**前端分享流程(建立連結→開新分頁貼上→實際顯示/作答/評分)還沒有真人瀏覽器實測**
- 已 commit(`28eece7`)+ subtree push 到 `steveedu711-oss/lunquiz`，Cloudflare Pages 自動部署中

## 下一步
- **尚未實機測試**:全部改動都只做過程式碼審閱+Node語法檢查(worker端API已用curl驗證),還沒有在真實瀏覽器打開驗證，下次接手第一件事：
  1. 手機/小螢幕排版(含造訪人次徽章)
  2. 出題紀錄開啟/刪除、複製設定連結帶入表單(含自訂欄位case)、單題重新出DOM替換與429冷卻UI
  3. **下載三格式**：PDF截圖效果(中文字型是否正常、分頁是否對齊A4、`.no-print`按鈕是否真的沒混進圖片)、Word/TXT內容是否完整
  4. **線上作答模式**：選擇/複選題判分是否正確、填空題文字比對的呈現是否清楚、手機觸控radio/checkbox/textarea的可用性、切分頁後重出題會不會正確更新作答表單
  5. **分享功能**：實際複製分享連結貼到另一個瀏覽器分頁(或無痕視窗模擬學生/家長)打開，確認`mode=student`看不到答案卷、`mode=key`可以看題目+解析、兩種模式下重新出題/下載/分享按鈕都正確隱藏、分享連結30天後過期是否符合預期(可先手動改小TTL測試，測完記得改回30天)

## ✅ 套用鴻綸品牌套件,新增品牌進場頁(splash-screen)(2026-07-24)
- 套用 `D:\ClaudeOnly\tools\hunglun-brand\` 品牌套件（對應Skill 209），把資源複製進 `public/assets/`：`brand.css`、`badge-medallion.png`、`logo-light.png`、`logo-dark.png`
- **`<head>` 新增**：Noto Sans TC + Material Symbols Outlined 字型連結、`<link rel="stylesheet" href="assets/brand.css">`
- **踩到的坑**：原始 `brand.css` 有一條全域 `* { font-family: 'Noto Sans TC', sans-serif; }`，會蓋掉lunquiz主功能頁原本的中文字型(Microsoft JhengHei/PMingLiU)——**已在複製進lunquiz的brand.css副本裡改成 `.hl-splash, .hl-splash *`** 只限定在品牌進場頁範圍內，不影響主頁面/列印/PDF的字型；只改了lunquiz這份副本，`D:\ClaudeOnly\tools\hunglun-brand\brand.css` 原始檔沒動
- **新增 `#hlSplash` 品牌進場頁**（`<body>`最上方，`#inputForm`原本內容整個改成預設`hidden`直到選擇後才顯示）：旋轉徽章 + 「小綸出題小幫手」標題 + 兩個選項按鈕：
  - 「🚀 直接進入（沒有自己的API Key）」→ 隱藏進場頁、顯示表單，就這樣進主功能，用內建額度
  - 「🔑 我有自己的API Key」→ 隱藏進場頁、顯示表單，並自動捲動+聚焦到 `#apiKeyBox`(原本的Key輸入區塊，已加id)，短暫加綠色外光暈提示使用者填在哪
  - 下方沿用品牌套件的logo lockup(淺色/深色自動切換用的`hl-logo-light`/`hl-logo-dark`兩張圖疊放)+作者信箱(`gfeh@hunglun.com`)/LINE聯絡按鈕(沿用hunglun-new官網上的同一組聯絡資訊)
- **分享連結流程不受影響**：`window.onload`偵測到 `?share=` 分享連結時，會直接把 `#hlSplash` 隱藏跳過進場選擇，讓學生/家長直接進到對應畫面，不用多選一次
- 已用 Node `new Function()` 驗證語法；**這批視覺改動同樣還沒實機瀏覽器測試**，尤其字型有沒有真的沒外溢到主表單、品牌進場頁在手機小螢幕的排版最需要確認

## 下一步(補充)
- 6. **品牌進場頁**：兩個按鈕點擊後是否正確切換、手機小螢幕下splash卡片版面是否正常、logo lockup 深/淺色圖有無跑版

## ✅ 全站字型統一改Noto Sans TC(2026-07-24)
- Steve糾正先前判斷：不要把Noto Sans TC限定在品牌進場頁範圍，**所有專案的中文字一律用Noto Sans TC**(Google通用字型)，已存全域記憶 `global_font_noto_sans_tc.md`
- `public/assets/brand.css` 的字型規則改回原本的全域 `* { font-family: 'Noto Sans TC', sans-serif; }`(不再限定`.hl-splash`範圍)
- `index.html` 的 `body`(整個表單頁)與 `.a4-page`(試卷/PDF)字型都改成 `"Noto Sans TC", <原本系統字型>, sans-serif`(保留Microsoft JhengHei/PMingLiU當fallback，避免使用者裝置字型載入失敗時完全沒有中文字可顯示)
- Word匯出(`dlDoc`)內嵌樣式字型也同步改成 `'Noto Sans TC', 'Microsoft JhengHei', 'PMingLiU', sans-serif`(Word程式本身可能沒裝Noto Sans TC，保留系統字型fallback)
- 已 commit(`c2a2f5a`)+ push，Cloudflare Pages 部署中；**這批字型改動也還沒實機看過視覺效果**，PDF/Word匯出的中文顯示尤其需要確認

## 下一步(補充2)
- 7. **全站字型**：實機確認Noto Sans TC有沒有正確載入顯示(而非fallback到系統字型)、PDF/Word匯出中文顯示是否正常

## ✅ UX優化：隱藏Key欄位/線上作答改開新分頁/使用說明(2026-07-24,Steve回報實測後提出)
- **選「直接進入(沒有Key)」自動隱藏API Key欄位**：`apiKeyBox`加`hidden` class，避免版面留空格造成混亂；留一條小連結「🔑 其實我有自己的API Key，改用這個」(`showApiKeyBox()`)可隨時叫回來，不用重整頁面
- **「📱 線上作答」改成開新瀏覽器分頁**：原本是`switchSheet('answer')`同頁切換分頁，Steve確認要改成「開新的瀏覽器分頁(新視窗)」。新函式`openAnswerModeNewTab()`：
  - **注意寫法**：`window.open('', '_blank')`必須在使用者點擊當下**同步**呼叫(先開空白分頁)，不能等`fetch`(建立分享)跑完才開，否則會被瀏覽器彈出視窗攔截擋掉；等fetch拿到分享ID後才把那個已開分頁的`location.href`導向正式網址
  - 背後其實是呼叫跟「分享題目」按鈕一樣的 `/api/share`(mode=student)，只是這邊直接開分頁而不是複製連結
  - 原本同頁切換的`switchSheet('answer')`/`answerModeContainer`邏輯保留，改給「分享連結載入」流程用(`loadSharedQuizIfAny`)，教師自己這頁不再用到
- **新增使用說明**：兩種都做了
  1. 表單上方常駐「① 填年級/科目/範圍 → ② 按小綸開始出題 → ③ 完成後可下載/列印/線上作答/分享」3步驟提示條
  2. 「❓ 使用說明」按鈕(表單右上角，跟出題紀錄/管理者同排)，點了彈窗顯示完整功能說明(快速開始/API Key說明/出題完成後能做什麼/注意事項)
- 已用 Node `new Function()` 驗證語法；**這批UX改動也還沒實機測試**，尤其開新分頁的彈出視窗攔截行為(不同瀏覽器/手機瀏覽器可能不同)最需要確認

## 下一步(補充3)
- 8. **UX優化**：「直接進入」隱藏Key欄位後小連結是否好找、「線上作答」開新分頁在手機瀏覽器(尤其是有彈出視窗攔截設定的)是否正常開啟、使用說明彈窗內容排版是否清楚

## ✅ 修正進度條%文字不動的bug + 三頁加官網連結(2026-07-24,Steve截圖回報)
- **bug原因**：出題時的進度條只有 `.style.width` 被更新(視覺寬度真的會變)，但條裡顯示的文字內容是HTML寫死的"0%"字串，從沒被JS改過，所以數字一直卡在0%沒動——新增共用函式 `setProgressBar(pct)` 同時更新 `style.width` 和 `textContent`，取代原本4處各自寫 `.style.width = X + "%"` 的地方
- **三個頁面都加了「有任何問題？請點鴻綸科技官方網站」連結**(指向 `https://hunglun-new.pages.dev/`)：
  1. 品牌進場頁(`#hlSplash`)：logo lockup下方、聯絡資訊上方
  2. 表單頁(`#inputForm`)：免責聲明區塊最下面
  3. 結果頁(`#resultSection`)：試卷/作答容器下方(深色背景配色跟結果頁一致，加了`no-print`不會印出來)
- 已用 Node `new Function()` 驗證語法；已 commit(`b5acfee`)+ push，Cloudflare Pages 部署中

## ✅ 修正線上作答判分抓不到正確答案的bug(2026-07-24,Steve截圖回報)
- **bug現象**：作答後不管選什麼都判定答錯，且「正確答案：」後面是空白
- **根本原因**：`getCorrectLetters()`原本假設AI回傳的`q.a`一定是字母(如"B")，用正規表達式抽取A-Z字元；但AI有時把完整選項文字當答案回傳(如`a:"眼睛看、鼻子聞、雙手觸摸"`)，中文字全部被正規表達式濾掉變成空字串，導致比對永遠失敗(不管使用者選哪個都不算對)、畫面也顯示空白
- **修法**：新增 `matchOptionLetter(q, rawAnswer)`，先拿`q.a`的文字去比對`q.o`選項清單找出對應字母，找不到才退回原本的字母萃取邏輯；`gradeAnswers()`顯示端也加保底，就算真的解析不出字母也會顯示原始答案文字而不是空白
- 已用Node直接測試修復後的邏輯，模擬Steve截圖的情境(完整選項文字當答案)確認能正確解析出字母
- 已 commit(`5a979ce`)+ push，Cloudflare Pages 部署中

## ✅ 新增儲存設定條件/清除所有條件按鈕(2026-07-24,Steve手機截圖提出)
- 需求：手機上每次都要重填年級/科目/範圍等出題條件很麻煩，要能存起來下次自動帶入，也要能一鍵清空
- 「🚀小綸開始出題」下方新增三顆按鈕一排：💾儲存設定條件／🗑️清除所有條件／🔗複製設定連結(原本就有，一起排版)
- `getFormSettingsSnapshot()`：把讀表單欄位的邏輯從 `copySettingsLink()` 抽出來共用，避免重複程式碼
- `saveDefaultSettings()`：存進 `localStorage.lun_saved_settings`；`loadSavedSettingsIfAny()`：頁面載入時套用(呼叫既有的 `applySettings()`)；`clearAllSettings()`：表單清空+砍掉已儲存的預設值(要`confirm`確認)
- **優先順序**：`window.onload` 判斷順序改成 分享連結(`?share=`) > 設定連結(`?grade=...`等URL參數，同事丟的複製設定連結) > 這台裝置自己存的預設值，避免同事丟連結給你結果被你自己的預設值蓋掉
- 使用說明彈窗也補了這兩個按鈕的說明
- 已用 Node `new Function()` 驗證語法；已 commit(`4f2fad2`)+ push，Cloudflare Pages 部署中，**還沒實機測試**

## ✅ 線上作答答錯時加顯示解析(2026-07-24,Steve截圖提出)
- 選擇/複選題答錯的回饋原本只顯示「正確答案：X」，沒有解析；填空/簡答題那邊本來就有附解析，這次補齊選擇題也一樣顯示 `｜解析：...`(用q.e欄位)
- 已 commit(`cd246d8`)+ push，Cloudflare Pages 部署中

## ✅ 修補儲存型XSS漏洞(2026-07-24,Steve要求優化檢視後發現)
- **問題**：全站沒有任何HTML escape函式，AI出的題目內容(`q.q`/`q.o`/`q.a`/`q.e`)在多處直接用`innerHTML`插入DOM；而`/api/share`端點完全沒有身分驗證，任何人都能直接POST任意JSON內容換到分享ID——等於任何人都能構造含惡意HTML/script的題目內容，做成分享連結送給學生/家長/老師，對方打開就會在lunquiz網域下執行任意JS(偷`localStorage`裡的裝置ID/已存設定等)
- **修法**：新增`escapeHtml()`工具函式(script區塊最上面，`getClientId()`之前)，把以下位置改成escape後再組字串插入`innerHTML`：`updateQuestionDOM()`(單題重出)、`renderPaperFixed()`(題目卷/答案卷/解析卷，含分享題目載入)、`renderAnswerMode()`(手機線上作答)、`openHistory()`(出題紀錄列表的標題)。`contenteditable`結構本身沒動，教師仍可自行編輯內容
- 已用Node `new Function()`驗證語法正確；**這批修改還沒實機測試**，下次接手優先確認：分享連結內容(含正常中文題目)顯示正常、嘗試分享含`<script>`或`<img onerror>`的title/題目內容確認畫面顯示的是逐字文字而非執行
- 尚未修：`/api/share`本身仍無速率限制(可被打爆KV空間，雖有500KB單筆上限)；`ADMIN_PASSWORD`未設定secret時會退回寫死在原始碼裡的預設密碼`5407`——這兩項待Steve確認是否要處理
- **已 commit(`7852d1f`)+ subtree push 到 `steveedu711-oss/lunquiz`**(過程中發現這次commit不小心把跟lunquiz無關、且是這台電腦上另一批已staged的`tools/gemini-auto`刪除變更一起包進去，已用`git reset --soft HEAD^`拆開重commit，只有lunquiz的2個檔案留在`7852d1f`；`ClaudeOnly`根目錄新增了`lunquiz` remote指向`steveedu711-oss/lunquiz.git`，push時一樣要`gh auth switch --user steveedu711-oss`，推完切回`safezone0619`)

## ✅ 線上作答新增答對/答錯回饋動畫(2026-07-24,參考school/el的做法)
- **參考來源**：`D:\ClaudeOnly\school\el`(React+framer-motion)的戰鬥模式，答對/答錯時題目卡有shake震動+色彩爆閃特效；lunquiz是純靜態HTML沒有framer-motion，改用純CSS `@keyframes`重現等效效果
- **新增CSS**(`<style>`區塊，`.progress-bar`規則後)：`lun-pop`(答對輕彈跳)、`lun-shake`(答錯左右震動)、`lun-flash-green`/`lun-flash-red`(外框光暈擴散淡出)、`lun-badge-pop`(✅/❌徽章彈入)，對應`.ans-card.ans-correct`/`.ans-card.ans-wrong`/`.ans-badge`/`.ans-score-pop`四個class
- **`renderAnswerMode()`**：每題卡片加上`id='ans-card-{i}'`+`class='ans-card'`，供評分後套動畫class用
- **`gradeAnswers()`**：新增`setAnswerFeedback()`(用`document.createElement`/`textContent`組回饋文字，不再用字串拼`innerHTML`，避免徽章功能重新引入XSS風險)與`flashAnswerCard()`(先移除舊class+強制reflow`void card.offsetWidth`再加新class，確保連續兩次評分都能重播動畫)；選擇/複選題按對錯觸發、填空/簡答題按文字比對結果(roughMatch)觸發同一套動畫。最終總分卡片也加了`ans-score-pop`彈入動畫
- 已用Node `new Function()`驗證語法；**這批動畫效果還沒實機測試**，下次接手優先確認：手機瀏覽器動畫流暢度、連續作答多題時動畫有無互相干擾、`ans-badge`彈跳動畫在CSS動畫疊加(class同時有pop+flash)時視覺是否協調
- 已commit(`50fa58b`)+ subtree push到`steveedu711-oss/lunquiz`

## ✅ 線上作答四項新功能：分享連結/雙模式/結果分享/學生資料欄位(2026-07-25,Steve截圖提出)
根據Steve截圖回報的4項需求逐一實作：

1. **學生作答連結更顯眼**：原本只有`分享答案(家長)`旁邊的`分享題目(學生作答)`按鈕做這件事，Steve可能沒注意到。在sticky header的`線上作答(開新分頁)`按鈕旁**新增`🔗複製學生作答連結`按鈕**(`btnShareStudentLink`)，直接呼叫既有的`shareQuiz('student')`，分享/家長模式下都會跟著隱藏(比照`btnAnswerMode`)
2. **雙作答模式(⚡即時/📋批次)**：`renderAnswerMode()`頂部新增radio選擇「⚡每題即時看結果」(預設)／「📋全部寫完再送出」。選擇/複選題的radio/checkbox加了`onchange='onAnswerChanged(i)'`，即時模式下選完馬上觸發該題判分+動畫；批次模式下onchange不動作，維持原本要按「送出評分」才看結果的行為。**填空/簡答題不論哪個模式都是送出評分後才判定**(文字比對本來就只能參考，沒有天然的「即時」觸發點，這算是刻意簡化，沒有另外做逐題檢查按鈕)
3. **分享結果連結給家長/老師**：`gradeAnswers()`重構出共用的`gradeOneQuestion(i)`(即時/批次共用同一套判分邏輯，結果存進`window.gradedDetails[i]`)。送出評分後總分卡片新增「📤分享結果給家長/老師」按鈕，呼叫新函式`shareResult()`把`{title, studentInfo, totalScore, maxScore, pct, details[]}` POST到**worker新端點`/api/result`**(比照`/api/share`的做法，存進既有`COOLDOWN_KV`，key前綴`result:`，30天過期，300KB上限，同樣**沒有身分驗證**)，換到結果ID組成`?result=ID`連結複製到剪貼簿。新增`loadResultViewIfAny()`+`renderResultView()`：偵測網址帶`result=`參數時，跳過表單直接顯示一頁式唯讀結果摘要(學生資料/總分/逐題對錯與解析)，不是完整試卷版面，所有內容照樣用`escapeHtml()`避免XSS
4. **姓名/年級/班級/座號欄位**：`renderAnswerMode()`新增一列4個input(選填，非必填，對應Steve確認的選項)，`shareResult()`送出時一併打包進`studentInfo`

**worker端變更**(`worker/src/index.js`)：新增`handleResultCreate`/`handleResultGet`兩個function+對應路由，邏輯完全比照既有`handleShareCreate`/`handleShareGet`(同一個KV、同樣30天TTL、同樣公開無驗證)
- 已用Node `new Function()`(前端)+`node --check`(worker)驗證語法皆正確；**這整批功能還沒實機測試**，下次接手優先確認：
  - worker新端點`/api/result`要先`wrangler deploy`才會生效(目前只是本地程式碼)
  - 即時模式選完選項後動畫+文字是否正確出現、選項能否重選並重新觸發判分
  - 送出評分後「分享結果」按鈕產生的連結，開新分頁貼上確認唯讀結果頁排版正常、選擇題/填空題的badge與參考答案顯示邏輯正確
  - 學生資料欄位選填不填也能正常送出分享
  - 手機瀏覽器：4個欄位窄螢幕排版、雙模式radio排版

## ✅ 全站UI質感/UX改版(2026-07-25,Steve要求「有質感一點」+改善UX)
用frontend-design skill先定調：不重新發明一套視覺語言，而是以`assets/brand.css`既有的鴻綸科技品牌(Google四色系統`--hl-blue/red/yellow/green`、圓角、柔和陰影)為基礎統一全站，解決兩個具體問題：

1. **按鈕配色是彩虹湯**：原本emerald/blue/purple/pink/orange/indigo/slate/gray七八種顏色各按鈕各用一色，沒有語意。改成`<style>`區塊新增一組`.lq-*` design token class：`lq-btn-primary`(藍，主要動作：小綸開始出題/下載/送出評分/歷史紀錄開啟)、`lq-btn-share`(綠，凡是「產生連結給別人」的動作：分享▾/分享結果)、`lq-btn-danger`(紅外框，清除/刪除類)、`lq-btn-secondary`(白底灰框，中性次要動作)、`lq-btn-ghost`(純文字連結感)、`lq-btn-dark`(sticky header深色背景專用，半透明白疊層+`is-active`高亮態)。全站已找不到殘留的`bg-purple/pink/orange/indigo`
2. **sticky header塞了9顆按鈕**：`題目卷/答案卷/線上作答開新分頁/複製學生作答連結/重新出題/下載▾/列印/分享題目(學生)/分享答案(家長)`擠成一排。改成：題目卷/答案卷合併成一個深色底的分頁式`is-active`切換群組；`複製學生作答連結`(上一輪新增的，其實跟`分享題目(學生作答)`功能重複)整併掉；`分享題目(學生作答)`+`分享答案(家長)`合併成單一「📤分享▾」下拉選單(仿照既有的「⬇️下載▾」下拉模式，新增`toggleShareMenu()`/`closeShareMenu()`)。從9顆按鈕減到6顆，且`btnAnswerMode`改名為「線上作答預覽」更精準(它其實是老師自己開新分頁測試用，不是給學生的連結)
3. **signature視覺**：進場頁徽章原本就有的品牌四色`conic-gradient`光暈旋轉效果(`hl-rotate-border`)，延伸到全站**唯一**一個地方——「🚀小綸開始出題」主按鈕新增`.lq-cta`class，hover/focus時外圈浮現四色光暈，呼應品牌識別又不濫用（其餘按鈕都保持安靜克制）
4. **其他一致性收尾**：表單所有input/select/textarea統一改用`.lq-input`(一致的圓角/邊框/focus態，focus時有品牌藍色光暈)；`section-title`(「1.請選擇出題模式」「2.詳細設定」)加上小圓點裝飾符；historyModal/usageGuideModal改用`.lq-card`+`backdrop-blur`背景；出題紀錄列表按鈕、線上作答的題目卡片/學生資料欄位/模式選擇列/分享結果卡片、結果分享唯讀頁全部改用`.lq-card`+`.lq-btn-*`統一視覺
5. 有加`@media (prefers-reduced-motion: reduce)`讓有動暈需求的使用者可以關閉所有CSS動畫(含之前那批答對/答錯動畫)
6. **`switchSheet()`函式簡化**：因為題目卷/答案卷/(舊的線上作答分頁tab)從3態簡化成2態(`btnAnswerMode`不再是tab而是獨立的預覽按鈕)，改用`classList.toggle('is-active', ...)`取代原本每次重寫整個className字串的寫法
7. 使用說明彈窗(`usageGuideModal`)內文同步更新，補上新的雙模式/學生資料欄位/分享結果連結說明，按鈕名稱也對應改成「線上作答預覽」「分享▾」
- 已用Node `new Function()`驗證script語法正確、`<style>`區塊大括號數量配對(94:94)；**這整批視覺改動完全還沒實機測試**，下次接手優先確認：手機/桌面瀏覽器實際排版、`.lq-cta`光暈動畫效果、下拉選單(下載▾/分享▾)在窄螢幕是否會超出畫面、深色sticky header在淺色系統主題下的對比度

## ✅ UI改版後Steve截圖回報7項細節修正(2026-07-25)
1. **進場頁「直接進入」按鈕文字太亂**：拿掉括號內的「（沒有自己的API Key）」，只留「直接進入」(對照組「我有自己的API Key」按鈕文字不變，兩者對比已經夠清楚，不需要額外註解)
2. **💾儲存設定條件/🗑️清除所有條件/🔗複製設定連結三顆按鈕只有一個有明顯效果**：根因是`.lq-btn-secondary`的預設`border-color:#e2e8f0`太淺，在白底幾乎看不見，導致只有`.lq-btn-danger`(紅色系border，對比夠強)看起來「有框」；改深成`#cbd5e1`，hover再加`box-shadow`，三顆現在視覺一致
3. **進度條要Google品牌色**：`.progress-bar`背景改成`linear-gradient(90deg, var(--hl-blue), var(--hl-red), var(--hl-yellow), var(--hl-green))`，取代原本的emerald雙色漸層
4. **sticky header的📱等emoji圖示很醜**：emoji在Windows上某些字體會退化成難看的方塊/縮小圖案(例如📱手機emoji本身畫了一排小格子app圖示，縮小後就是一坨色塊)。改用`<head>`早就載入的Material Symbols Outlined字型，新增`.lq-icon`class，把重新出題/線上作答/下載/列印/分享按鈕、下載選單(Word/PDF/txt)、分享選單(學生/家長)的emoji全部換成`refresh`/`smartphone`/`download`/`print`/`ios_share`/`description`/`picture_as_pdf`/`article`/`school`/`family_restroom`等Material Symbol
5. **「線上作答預覽」改名「直接線上測驗」**：按鈕文字與使用說明彈窗內文同步更新
6. **分享連結要能分辨對應哪份題目/哪個學生**：`shareQuiz()`複製到剪貼簿的內容從純網址改成`【題目名稱】學生作答連結\n網址`格式(貼到LINE等會自動把網址部分變超連結，前面的題目名稱純文字保留)；`shareResult()`同理改成`【題目名稱】姓名/年級班級座號 的作答結果\n網址`，姓名沒填就退回顯示年級班級座號組合
- **順手修掉一個因為加圖示才出現的bug**：`shareQuiz()`原本用`event.target`(可能點到圖示span本身而不是按鈕)+`btn.innerText`(還原時會把圖示的`<span>`結構整個清空、退化成純文字)，改成`event.target.closest('button')`+`btn.innerHTML`捕捉/還原，才不會分享一次連結後圖示就消失
- 已用Node `new Function()`驗證語法、`<style>`大括號配對(95:95)；**這批修正也還沒實機測試**，下次接手優先確認：Material Symbols字型有沒有正確載入顯示(而非顯示成文字ligature名稱如"school"字樣，代表字型沒載入成功)、進度條Google四色漸層在窄小%數時的視覺效果、分享訊息文字排版

## ✅ 修正第2點：hover效果真正的根因(2026-07-25,Steve再截圖回報「只有中間的清除按鈕有效果」)
- 上一版把`.lq-btn-secondary`的border加深後，Steve截圖顯示三顆按鈕靜態畫面確實都看得到邊框了，但**滑鼠移過去(hover)時只有中間的「清除所有條件」(danger)有明顯反應**——真正原因是每個變體的hover效果強度天差地遠：`.lq-btn-danger:hover`整塊填滿紅色(視覺變化很大很好認)，`.lq-btn-secondary:hover`卻只有邊框跟背景色微調(變化很小，肉眼幾乎看不出來)，同一排放在一起自然像「只有一個有效果」
- **修法**：在`.lq-btn`基礎class新增`.lq-btn:hover:not(:disabled) { transform: translateY(-1.5px); }`，讓**所有變體**hover時都有一致的「上浮」位移回饋，不再只靠各自的顏色變化強度；`.lq-btn-secondary`跟`.lq-btn-danger`也都補上預設`box-shadow:var(--lq-shadow-sm)`+hover時加重陰影，兩者現在的hover視覺重量對等。`.lq-btn-ghost`(出題紀錄/使用說明等輕量文字連結按鈕)刻意用`transform:none`排除在上浮效果外，維持該款式原本低調的文字連結感(不在這次被投訴的3按鈕之列)
- 已用Node驗證語法、`<style>`大括號配對(96:96)；**還沒實機測試**，下次接手優先確認三顆按鈕hover時上浮幅度是否自然、其他頁面用到lq-btn的按鈕(sticky header/送出評分等)hover效果會不會因為新增的上浮位移跟原本的active縮放(scale 0.97)疊加出怪異效果

## ✅ worker部署卡關解除+`/api/result`已上線(2026-07-25)
- **根因**：環境變數`CLOUDFLARE_API_TOKEN`(設定在`SteveConfig/secrets.ps1`)已失效，`wrangler`優先吃env token導致`wrangler login`本身都會先擋下(`You are logged in with an API Token. Unset the CLOUDFLARE_API_TOKEN...`)
- **解法**：該次shell session先`unset CLOUDFLARE_API_TOKEN`，改用`wrangler login`走瀏覽器OAuth重新授權成功，再用同一個session(同樣先unset)跑`wrangler deploy`部署成功。**這只解掉這個session/這次終端機的認證，`secrets.ps1`裡失效的`CLOUDFLARE_API_TOKEN`本身沒有更新**，下次開新terminal如果env var還是舊值，`wrangler`指令一樣會先用失效token卡住，需要重複`unset`或請Steve更新`secrets.ps1`裡的token
- 已用curl實測`/api/result`建立→讀取往返正常(JSON結構完整，中文在Git Bash終端機顯示亂碼是純顯示問題非資料錯誤)
- **分享結果連結功能現在可以正常使用了**，卡關點解除

## ✅ 列印續頁邊界+設定按鈕/sticky header質感重做(2026-07-25,Steve截圖回報3項)
- **列印邊界bug**：題目多、單一`.a4-page`超出297mm時瀏覽器自動分頁出的續頁，原本邊界只掛在`.a4-page`元素本身的padding上，對自動分頁續頁無效(續頁會貼齊紙張邊緣)。改用`@page { margin: 10mm 12.5mm }`統一交給每一張實體紙自己套用，`.a4-page`本身print時padding改0(避免疊加兩次)
- **儲存/清除/複製設定連結三顆按鈕**：改用圓形色塊icon(藍/紅/綠)+hover灌滿實色的`.lq-btn-tool`系列class，取代原本純文字細邊框樣式
- **sticky header**：改漸層背景+底部品牌四色細線，這版做得太保守(Steve反饋「按鈕沒有變漂亮」)，下一輪加重對比後才有感

## ✅ 出題後試卷空白的bug修正(2026-07-25,Steve截圖回報「生成題目後變成這樣」)
- **根因**：跟UI改版無關，是`startGen()`裡原本就有的bug——AI回傳JSON解析失敗時只有`console.error`，完全沒有使用者提示；若所有批次都解析失敗，`allItems`會是空陣列，但`renderPaperFixed()`一開始就會把`resultSection`從hidden切成顯示，所以畫面會切到結果頁、header正常顯示，但因為沒有任何題目資料，試卷內容整個空白，也沒有任何錯誤訊息
- **修法**：全部批次都解析失敗時改成`throw Error`，會被外層catch的`alert()`接住提示重新出題；部分批次失敗則跳警告但保留成功的題目繼續顯示，不整批放棄

## ✅ 全站icon改用Phosphor Icons Duotone(2026-07-25,Steve要求「先搜尋好看的icon再改版」)
- Material Symbols Outlined的細線條icon在深色sticky header上顯得普通。查了Lucide/Phosphor/Tabler三家比較後選**Phosphor Icons的Duotone(雙色調)** weight，質感更明顯
- CDN改用`https://cdn.jsdelivr.net/npm/@phosphor-icons/web@2.1.2/src/duotone/style.css`，class格式`ph-duotone ph-{icon名}`(icon本身是空`<span>`靠`::before`偽元素畫圖，不像Material Symbols要在裡面塞英文icon名當文字內容)
- 全站16處icon都換了(進場頁rocket_launch/vpn_key/mail、設定按鈕列save/trash/link、sticky header的重新出題/線上測驗/下載/列印/分享、下載選單Word/PDF/txt、分享選單學生/家長)，每個icon名稱都先用curl比對Phosphor官方GitHub清單(`phosphor-icons/core`repo的`assets/regular/`資料夾)確認存在才換，避免顯示不出來
- sticky header同時加重了視覺對比：分隔線加深、底部四色線加粗、重新出題/線上測驗/下載icon依語意上色(amber/sky/blue)，回應Steve「按鈕沒有變漂亮」的反饋
- 已用curl實測Phosphor duotone CSS檔案本身、以及所有需要的icon glyph規則(`.ph-arrow-clockwise:before`等16個)都確實存在於CDN上；**icon視覺效果本身還沒實機看過**(無法自動化操作瀏覽器)，下次接手第一件事：確認icon是否正確顯示成圖形(而非顯示不出來變空白，或顯示成沒有樣式的方塊)

## 下一步(補充4)
9. **這次全部改動的實機驗證**：Phosphor icon是否正確顯示、sticky header新配色對比是否OK、設定按鈕列圓形色塊icon效果、列印/PDF下載時題目超過一頁的續頁邊界是否正常、出題失敗時的alert提示文字是否清楚

## ✅ 線上作答加「確認這題」按鈕避免誤按(2026-07-25,Steve截圖回報)
- 即時模式原本選radio/checkbox的onchange就馬上判定，誤點旁邊選項也被當成已作答。改成選完要按每題底下的「✅確認這題」才呼叫`gradeOneQuestion()`，判定後鎖住該題選項+隱藏按鈕。此按鈕只在即時模式顯示(`startAnswerQuiz()`依模式決定)，批次模式不受影響
- 已curl確認`d9efe00`部署成功、線上出現`confirmAnswer`

## ✅ 新增「即時進度」功能：老師/家長可即時觀看學生線上作答進度(2026-07-25/26,完整Plan模式走過一輪)
跟Steve討論設計方向(先子代理評估技術方案，決定輕量輪詢版不用WebSocket/Durable Objects，避免超出Cloudflare免費方案)，進Plan模式寫計畫核准後實作：

**關鍵限制**：Cloudflare Workers KV免費方案每天只有1000次寫入額度(read是100,000/day)，超過只會讓寫入操作失敗，**不會自動觸發付費**(已查證，非自動升級付費方案)。設計核心原則：寫入頻率綁定「題目數量」不是「時間」，並加自我限流的每日預算保護。

**Worker新增**(`worker/src/index.js`)：
- `POST /api/live-progress`：學生端寫入進度，沿用既有`COOLDOWN_KV`，key `live:{shareId}:{studentId}`，value存完整進度(給家長查看)，`metadata`存精簡摘要(老師儀表板`list()`一次拿到全班摘要，不用逐一`get()`)，8小時TTL
- `GET /api/live-progress/:shareId`：老師儀表板用，**需要`X-Admin-Pass`驗證**(比照既有`handleKeysCheck`寫法，因為會列出全班姓名+分數)
- `GET /api/live-progress/:shareId/:studentId`：家長單人視角，比照`/api/share`、`/api/result`的capability-URL哲學不需密碼
- **每日寫入預算保護**：`live-budget:{今天日期}`計數key(25小時TTL自動歸零)，上限`LIVE_PROGRESS_DAILY_WRITE_CAP=400`(因計數本身也要寫一次KV，實際約消耗800次寫入，留200次給既有的冷卻/分享/分享結果/訪客數功能)，超過上限**靜默停止**(回`{throttled:true}`，200非錯誤)，學生作答本身完全不受影響
- 已用curl完整測試7種情境全部通過：寫入/缺欄位400/預算保護/list+metadata/admin密碼驗證(缺密碼401)/單人查詢/查不存在404

**前端新增**(`public/index.html`)：
- `getLiveStudentId()`：比照`getClientId()`但用`sessionStorage`(key `lun_live_student_id`)，換分頁視為新學生，同分頁重整不重複
- `getOrCreateShareId()`：讓同一份題目(`window.currentItems`參考位址判斷)重複分享(學生連結/家長連結/儀表板)共用同一個shareId，不用另建「批次」概念；`shareQuiz()`已改用這個共用函式
- `pushLiveProgress()`：選擇/複選題`onchange`(新函式`onAnswerPicked`，**不做判分**，跟`confirmAnswer`的判分邏輯分開)、文字題`onblur`且內容真的有變(`onTextAnswerBlur`)時才推送，只有`window.isSharedView && sharedMode==='student'`(透過`?share=`連結進來)才會動作，fire-and-forget不影響作答
- `shareLiveProgressLink()`：學生開始測驗後，題目區塊上方新增「📤分享即時進度連結給家長」按鈕，組`?live=shareId&student=studentId`連結
- `shareLiveDashboardLink()`：sticky header的「分享▾」選單新增「即時進度儀表板」項目，組`?live=shareId`(不含student=老師模式)
- `loadLiveViewIfAny()`：`window.onload`新增最優先判斷分支(在`?result=`之前)，偵測`?live=`參數，依有無`student`參數分流到`renderLiveTeacherDashboard()`(要求輸入管理者密碼，沿用既有`sessionStorage.linquiz_admin`機制，9秒輪詢)或`renderLiveParentView()`(不用密碼，7秒輪詢)；兩者都在`visibilitychange`分頁背景時暫停輪詢省額度

**驗證狀態**：worker端已完整curl測試通過；前端邏輯**還沒實機瀏覽器測試**(無法自動化操作瀏覽器)，下次接手優先確認：
1. 老師分享「學生作答連結」後，另外複製「即時進度儀表板」連結(分享▾選單)，開新分頁輸入管理者密碼(5407)能否看到儀表板
2. 無痕視窗模擬學生開作答連結、填資料、開始測驗、選幾題後，儀表板8-10秒內是否看到進度更新
3. 學生端點「分享即時進度連結給家長」，第三個分頁貼上該連結，確認只看得到這位學生的進度(不用密碼)
4. 手機瀏覽器：分享▾選單新項目排版、儀表板/家長視角在小螢幕的呈現
- **commit小插曲**：commit時因為沒有先檢查`git status`，不小心把另一個並行Claude session(3dweb-demo專案)已staged的檔案一起包進commit，已用`git reset --soft HEAD^`+`git commit -- <指定路徑>`拆開修正，只留lunquiz的2個檔案在最終commit，對方staged的檔案完全沒被動到。**教訓：commit前務必先`git status`確認staged內容，尤其在多專案monorepo、可能有並行session的情況下**
- 已commit(`d90adb6`)+ subtree push(`2479df7`)到`steveedu711-oss/lunquiz`，worker已`wrangler deploy`部署，Cloudflare Pages build成功並已用curl確認前端內容上線

## ✅ 實機Playwright完整驗證(2026-07-27,首次真的用瀏覽器測過,非僅Node語法檢查)
用`mcp__playwright__*`工具(claude-in-chrome全域禁用,改用Playwright)實測正式站`https://lunquiz.pages.dev`,走完整流程：品牌進場頁→表單→出題→題目卷/答案卷切換→下載(Word/PDF/txt)→分享(學生連結/結果連結/即時進度儀表板)→線上作答判分。

**全部正常的項目**：
- 品牌進場頁兩按鈕、造訪人次計數器、Noto Sans TC字型(`document.fonts`確認`status:loaded`)、Phosphor Duotone icon(`ph-duotone`偽元素`font-family`確認載入)、儲存/清除/複製設定連結三按鈕邊框與hover上浮效果
- 實際出題(國小五年級/自然/第一課 植物的構造/5題單選/簡單)：進度條、題目卷內容、答案卷(解析卷)都正常顯示，非空白
- txt/PDF下載都成功產出且檔名正確帶中文；Word下載用`showSaveFilePicker`原生存檔對話框，自動化環境會被系統靜默取消(非bug，真實使用者操作OK，只是這個管道沒辦法在無人值守自動化下驗證到底)
- 分享題目(學生作答連結)：開新分頁`?share=xxx&mode=student`正確隱藏老師按鈕、顯示線上作答表單
- 線上作答即時模式：選對/選錯即時判分+綠框「答對了」/紅框「答錯了+正確答案+解析」動畫都正確觸發，5題(4對1錯)送出後正確算出80/100分
- 分享結果連結`?result=xxx`：唯讀頁正確顯示學生資料(五年級・測試學生)、總分、逐題對錯與解析
- 即時進度儀表板`?live=xxx`：管理者密碼(5407) prompt驗證通過後，正確顯示「五年級・測試學生 5/5題」即時進度條，跟學生實際作答狀態一致——這是上一輪最大的新功能，端到端測試成功

**發現2個小bug，已於同日修好並發布**：
1. **PDF匯出中文吃字**→已修：`dlPDF()`用html2canvas截圖，第3題解析「（或稱直根系）」這段文字在PDF裡被吃字/亂碼成「 载稱直根系）」(比對同一份內容的txt下載，文字完全正常，證實是html2canvas截圖渲染的問題，不是資料本身錯誤)。根因研判是html2canvas截圖當下Noto Sans TC字型還沒完全載入(FOUT)，用fallback字型的字元寬度截圖造成錯位疊字；修法是`buildPdfDoc()`截圖前加`await document.fonts.ready`
2. **`?result=`結果頁footer重複**→已修：`renderResultView()`自己又組了一次「有任何問題？請點鴻綸科技官方網站」插入`#paperContainer`，跟`#resultSection`本身常駐的同樣footer疊加顯示兩次。刪掉`renderResultView()`裡那行內建的footer，重用常駐的即可

## ✅ 列印頁首頁尾問題+AI解析失敗頻繁,兩輪回報修正(2026-07-27,Steve截圖回報)

**問題1：列印跑出瀏覽器內建日期/標題/網址/頁碼，且貼近邊緣**
Steve截圖回報列印出來上方有「2026/7/27 下午1:35」「小綸出題小幫手」，下方有「lunquiz.pages.dev」「2/4」，且內容貼邊。這些其實是Chrome瀏覽器`window.print()`內建的「頁首和頁尾」列印選項自動加上的(日期+標題在上、網址+頁碼在下，跟Chrome的固定格式完全吻合)，不是網站程式碼產生的文字。第一輪先嘗試：`@page margin`從10mm加大到18mm留緩衝、使用說明加提醒使用者去列印視窗「更多設定」關掉「頁首和頁尾」——但Steve不接受這個說法(他表示原本列印沒有這些文字)，且教使用者去改瀏覽器設定本來就是體驗很差的解法。

**最終解法(已採用)**：徹底不再用`window.print()`。「🖨列印」按鈕改名「下載並列印」，改叫`printPDF()`：
- 抽出`buildPdfDoc()`共用PDF產生邏輯(html2canvas截圖+jsPDF組頁)，`dlPDF()`(下載存檔用)和`printPDF()`(新分頁列印用)共用同一段
- `printPDF()`比照`openAnswerModeNewTab()`的寫法：`window.open('', '_blank')`要在await之前同步呼叫(先開空白分頁)，避免PDF產生完才開被瀏覽器彈出視窗攔截擋掉；產生完後把該分頁導向PDF的`bloburl`，並`setTimeout`800ms後呼叫該分頁的`win.print()`自動叫出列印視窗
- 印的是PDF文件本身(不是HTML網頁)，Chrome的「頁首和頁尾」選項只對網頁列印有效，對PDF檢視器列印不生效，所以徹底不會再出現日期/網址/頁碼——已用Playwright實測，新分頁正確開啟PDF內容(國小五年級自然試卷5題)，PDF檢視器本身工具列右上角就有列印圖示可用，無彈出視窗攔截問題，console無錯誤
- 彈出視窗被攔截時會alert提示改用「⬇️下載 ▾ → PDF」

**問題2：「AI回傳的內容無法解析成題目格式」常出現**
追查worker呼叫Gemini時`generationConfig.maxOutputTokens`只設8192，題數多(一批10題)或計算/簡答題解析寫長時容易被截斷，導致JSON陣列沒收尾，整批`JSON.parse`失敗直接跳alert放棄。已修：
- worker端`maxOutputTokens`提高到32768，降低被截斷的機率(已`wrangler deploy`)
- 前端解析失敗時新增搶救機制：用正規表達式`/\{"q":.*?"s":\s*\d+\}/g`把已經完整的題目物件一個個挖出來(即使陣列外層因截斷而不合法)，只有真的一題都救不回來才會跳錯誤alert跟丟進`parseFailBatches`

## 卡關點
- 無(2026-07-27)。「下載並列印」流程已用Playwright實測PDF能正確在新分頁開啟；`win.print()`本身在無人值守自動化瀏覽器環境下無法驗證是否真的跳出系統列印對話框(這部分只能靠Steve實機確認)，下次接手如果Steve還是回報列印怪異，優先確認是不是彈出視窗被攔截導致`printPDF()`走進alert分支
- AI解析失敗降頻是否真的有感，需要Steve後續多次實際出題後才能驗證，如果拉高token上限後還是常出現，下一步可以考慮把大題數的`batchSize`降低(目前單選/複選是10、填空簡答是8)
- (舊)無(2026-07-26)。若下次遇到`wrangler`指令又出現`Authentication error`,先檢查是不是`secrets.ps1`裡的舊`CLOUDFLARE_API_TOKEN`又被載入,同樣手法(`unset`後`wrangler login`)可解
- **git push流程注意**：ClaudeOnly是多專案monorepo，同時可能有其他專案(如hunglun-new、3dweb-demo)在跑`git`操作，可能有並行session已staged自己的檔案；commit前**務必先`git status`**確認只有預期的檔案，不要盲目`git add <path> && git commit`；遇到`.git/index.lock`錯誤，先用`Get-CimInstance Win32_Process -Filter "Name='git.exe'"`確認是否真的有進程在用**同一個worktree**(linked worktree如`ClaudeOnly-subtree-tmp`各自有獨立index，不會互卡)，確認沒有才能刪lock；`git subtree push`常需要2分鐘以上，記得用`run_in_background:true`避免逾時被砍
- **subtree push偵測變化的邏輯**：只有真的改到`school/lunquiz/`底下檔案內容的commit才會被subtree push偵測到並推上去；空commit(`--allow-empty`)不會產生新的subtree樹狀態，會顯示「Everything up-to-date」但實際沒推東西上去，要重試部署必須對該路徑下的檔案做出真實變動

## ✅ 新增帳號系統：學生/家長/老師登入+班級出題歷史(50份上限)+監考式即時監看(2026-07-27,Steve提出需求→Plan模式規劃→實作→Playwright實測)

### 需求釐清(AskUserQuestion三輪)
- 登入方式：帳號密碼自己存(不用Google登入/邀請碼)
- 帳號建立：自由註冊，任何人可選學生/家長/老師其中一種角色(超級管理員不開放自助註冊，避免自封)
- 存歷史範圍：每個**班級**50份上限(不是每老師/全站)
- 登入跟現有分享連結(?share=xxx)並存，不取代：老師登入後多一項「存入班級歷史」，學生/家長不登入一樣能用分享連結作答/查看
- 即時監看要求：不只是進度%，要看到學生「目前正在作答的題目內容」(監考式)

### 架構
**新增Cloudflare D1資料庫**(`lunquiz-db`, binding `DB`)存帳號/班級/歷史：
- `users`(id/username/password_hash/salt/role/display_name/class_id)：role限定'student'/'parent'/'teacher'/'superadmin'
- `classes`(id/name/teacher_id)
- `parent_child_links`(parent_id/student_id)：家長輸入孩子username建立連結,不需孩子核准
- `quiz_history`(id/class_id/title/items_json/created_by/created_at)：每班超過50筆自動刪最舊的
- migration檔：`worker/migrations/0001_init.sql`，已用`wrangler d1 execute --remote`建表

**即時監看沒有搬去D1，沿用既有KV(`COOLDOWN_KV`)機制**：只是把`live:{shareId}:{studentId}`存的value從「已答題數」擴充成每題實際選的選項字母/填的文字(`answers:[{i,selected}]`)。老師總覽(`list()`)仍只顯示進度%(metadata維持輕量)，點某位學生「查看詳情」才會額外`fetch`該份題目內容(`/api/share/:id`)+單人live-progress，前端組合渲染成監考式逐題畫面。

**帳號系統跟即時監看是兩個獨立ID空間，額外做了一個橋接**：家長要監看孩子，得知道孩子當下在哪個shareId作答，但這在原設計裡無法得知。解法：已登入的學生作答時，`pushLiveProgress()`額外帶上`studentUsername`，worker端多寫一個KV指標`student-active:{username}` → `{shareId,studentId}`(8小時TTL跟live-progress一致)。家長透過新端點`/api/parent/child-live/:username`(需驗證parent_child_links已連結)查這個指標，直接拿到即時進度，不需要另外要分享連結。**限制**：只有學生剛好有登入帳號時才會被記錄到，學生登入非強制，若學生沒登入純用分享連結作答，家長端目前無法監看(這是刻意的設計取捨，符合「登入非必要」的需求)。

**密碼雜湊**：`crypto.subtle`的PBKDF2(Workers runtime原生支援，不需額外套件)，100000次疊代+隨機salt。
**Session token**：HMAC-SHA256簽章的`{uid,role,exp}`，存`localStorage.lun_session_token`，每次請求帶`X-Session-Token` header(比照現有`X-Admin-Pass`的header-based做法，不用cookie，因為現有CORS是`Access-Control-Allow-Origin:*`沒開credentials)。30天過期。

### 新增Worker API(`worker/src/index.js`)
`/api/auth/register`、`/api/auth/login`、`/api/auth/me`、`/api/history`(GET列表/POST新增)、`/api/history/:id`(DELETE)、`/api/parent/link-child`、`/api/parent/children`、`/api/parent/child-live/:username`

### 前端改動(`public/index.html`)
- 表單頁header新增「👤登入/註冊」按鈕+登入狀態顯示，點開`accountModal`彈窗(登入/註冊表單，或已登入時顯示帳號資訊+班級ID(老師)/連結孩子(家長)+登出)
- sticky header新增「💾存入班級歷史」按鈕，只有`currentUser.role==='teacher'`時顯示
- 新增`classHistoryModal`班級歷史列表(開啟/刪除)
- 新增`proctorModal`監考式詳情，老師從即時進度儀表板每位學生卡片可點「🔍查看詳情」，家長從「我的孩子」列表點「🔍即時監看」，兩者共用`renderProctorAnswersHtml()`渲染邏輯，每5秒輪詢更新
- `pushLiveProgress()`擴充送出`answers`陣列(選擇題送選中字母組合、文字題送內容)+`studentUsername`(僅登入學生會帶)

### Playwright端到端實測(全部通過)
1. 老師註冊(帳號+班級一起建)→顯示班級ID→出題→存入班級歷史→重新整理頁面確認session持久化(localStorage)→開啟班級歷史清單確認題目還在→點開啟重新載入試卷
2. 學生用老師給的班級ID註冊成功(用假班級ID會擋下"找不到這個班級ID")
3. 直接呼叫API模擬「已登入學生推送真實作答內容(帶studentUsername)」→家長註冊→連結孩子帳號→呼叫`child-live`端點→**正確拿到`answers:[{i:0,selected:"B"}]`跟推送內容一致**
4. 前端`openProctorDetailParent()`實際渲染監考畫面，正確顯示「1. 1+1=? ✍️(B) 2」「2. 2+2=? 尚未作答」，題目內容跟作答內容正確對應
5. 安全性測試：家長查詢未連結的學生帳號正確擋下(403「尚未連結這個學生帳號」)

### 過程中發現並修復的2個bug
1. register/login API回應沒帶老師的`className`，導致註冊/登入後帳號modal只顯示班級ID沒有班級名稱→已補上
2. 已登入時右上角帳號按鈕原本設計成直接觸發登出，導致登入後沒辦法再點開帳號modal查看班級歷史/連結孩子→改成一律開modal，登出動作收進modal內的按鈕

### 下一步(舊，部分已在下方新一輪解決)
- **superadmin帳號**：目前完全沒有介面建立，也沒有專屬管理功能，Steve若要用需先自己跑`wrangler d1 execute`手動塞一筆(密碼雜湊要先在本機用PBKDF2算好)——下次可以考慮做個最基本的管理頁面
- **測試資料已清除**：驗證用的teacher_pw1/student_pw1/parent_pw1帳號跟班級/歷史記錄已用`DELETE FROM`清空，D1目前是乾淨狀態
- ~~學生線上作答頁面沒有登入入口~~ → 已在下一輪解決

## ✅ 學生作答頁加登入入口+帳號UX改善+班級ID改用班級名稱當代碼(2026-07-27)

### 1. 分享作答頁新增學生登入入口(解決上一輪的下一步)
- `renderAnswerMode()`開始畫面最上方(學生資料欄位之前)新增`ansLoginBox`區塊，只在`window.isSharedView && sharedMode==='student'`時顯示
- 未登入：「🔑 登入學生帳號以啟用家長即時監看」按鈕，開既有`accountModal`；已登入(學生角色)：綠框顯示「✅已登入：xxx，家長可用這組帳號連結監看」+登出按鈕；登入但非學生角色：提示需要學生帳號
- `updateAccountUI()`(login/register/logout都會呼叫)串接新函式`refreshAnswerLoginBox()`，登入狀態一變就即時更新，不用重整頁面
- 已commit(`0c322a1`)+ subtree push(`ee3b1b5`)

### 2. 帳號UX修正(Steve截圖回報：註冊沒有密碼二次確認、班級ID不該用數字)
- **密碼二次確認**：註冊表單新增`authPasswordConfirm`欄位，`doRegister()`送出前檢查兩次密碼是否一致
- **班級ID改選填**：worker `handleAuthRegister`學生分支只在有填`classCode`時才驗證，沒填一樣能註冊成功(class_id為null)
- **新增`POST /api/auth/join-class`**：登入學生之後可在「我的帳號」補填/更換班級代碼
- **班級ID改用班級名稱本身當唯一代碼**(Steve提出「不能用1年1班這種嗎」)：老師註冊時填的班級名稱(如「六年1班」)本身就是學生加入用的代碼，不再是內部numeric ID；新增migration`0002_class_name_unique.sql`對`classes.name`加唯一索引，重複名稱註冊會被400擋下(提示換更完整名稱如加學校/年度)
- 前端所有`authClassId`/`joinClassId`欄位改名`authClassCode`/`joinClassCode`，老師帳號頁顯示改成「班級代碼：六年1班」不再顯示內部數字ID

### 3. 部署與驗證
- D1 migration已用`wrangler d1 execute --remote`跑過(unset舊`CLOUDFLARE_API_TOKEN`後執行，同樣的已知坑)
- worker已`wrangler deploy`；已用curl完整測試：老師註冊建班→重複班級名稱正確400擋下→學生免填班級代碼註冊成功→join-class用錯誤/正確班級代碼分別測試都正確
- **驗證用的4個測試帳號(teacher_test/teacher_dup/student_test2/student_test3)+2個測試班級(id 4,5)已用`DELETE FROM`清空**，D1回到乾淨狀態
- 前端已commit(`1e63a27`)+ subtree push(`e60011e`)到`steveedu711-oss/lunquiz`，Cloudflare Pages自動部署

### 4. 使用說明書
- 用Artifact產出一份完整的獨立網頁使用說明書(涵蓋不登入/老師/學生/家長登入後差異+FAQ)，是私人連結，Steve可自行決定要不要分享出去，**沒有嵌入前端**，純粹是額外參考文件
- **前端內建的`usageGuideModal`(❓使用說明按鈕)也同步補上帳號登入段落**：新增「👤 帳號登入（選用）」區塊說明老師/學生/家長登入後各自多的功能，也補充「忘記密碼目前無法自助重設」的提醒

### 下一步
- ~~這批新增的`join-class`/`classCode`改動、`ansLoginBox`登入區塊、`usageGuideModal`新段落還沒實機測試~~ → **已於2026-07-28用Playwright實機驗證全部通過**，詳見下方新增章節
- superadmin管理頁面、忘記密碼重設功能：都還沒做，Steve尚未要求就先不動

### 卡關點
- 無(2026-07-28)

## ✅ Playwright實機驗證上一輪(2026-07-27)帳號系統4項新功能，全部通過(2026-07-28)
用`mcp__playwright__*`工具(claude-in-chrome全域禁用)對正式站`https://lunquiz.pages.dev`實測，4項全部正常，無bug：

1. **老師註冊+班級代碼**：註冊(pw_teacher_20260728/班級「驗證用測試班A」)成功，帳號modal正確顯示「班級代碼：驗證用測試班A」
2. **重複班級名稱擋下**：第二個老師(pw_teacher2_20260728)用同班級名註冊，正確顯示inline錯誤「這個班級名稱已經被使用，請換一個更完整的名稱（例如加上學校或年度）」，且沒有多產生一筆班級記錄(D1查證class_id=6只有一筆)
3. **分享連結學生登入modal**：出題→分享▾→學生作答連結→開新分頁`?share=xxx&mode=student`，登入按鈕「🔑登入學生帳號以啟用家長即時監看」正確顯示；註冊學生帳號(pw_student_20260728，班級代碼欄位選填)成功後，登入box即時更新成「✅已登入：驗證學生...」(不用重整頁面)
4. **學生補填班級代碼(join-class)**：「我的帳號」用錯誤班級名稱「不存在的班級XYZ」測試，正確跳alert「找不到這個班級代碼，請跟老師確認名稱是否正確」；改填正確名稱「驗證用測試班A」後成功，modal正確顯示「班級：驗證用測試班A」
5. **使用說明彈窗新段落**：捲動到「👤帳號登入（選用）」段落，排版正常無overflow/重疊，老師/學生/家長三段文字清楚
6. **額外確認**：登入session透過`localStorage`跨分頁(同origin)正確共用，切回主分頁也顯示已登入狀態

**驗證用測試資料已清除**：D1裡的`pw_teacher_20260728`/`pw_student_20260728`兩個帳號+`驗證用測試班A`班級記錄，已用`wrangler d1 execute --remote`的`DELETE`清空(Steve確認後執行)，正式站D1恢復乾淨

## ✅ 進場頁身分選擇改版 + 超級管理員後台(2026-07-28，Plan模式規劃後實作)

Steve對進場頁截圖提出需求：把「直接進入／我有自己的API Key」兩鍵版，改成先選身分「快速使用(不登入)／學生／家長／老師／管理員」，除了快速使用其餘都要帳號密碼登入/註冊，之後才決定要不要用自己的API Key。「管理員」延伸出全新的**超級管理員後台**需求(可看全站帳號、改角色、重設密碼、刪除帳號)，比原本進度只記錄「superadmin完全沒有介面」更進一步。

### 前端(`public/index.html`)
- `#hlSplash`改成三步驟卡片(同一張卡片內切換，不跳頁)：`#hlStepIdentity`(快速使用大按鈕+2x2身分卡片grid，用Phosphor duotone icon：`ph-student`/`ph-users-three`/`ph-chalkboard-teacher`/`ph-shield-check`，都先用curl比對Phosphor官方GitHub確認存在)→`#hlStepAuth`(內嵌登入/註冊小表單，角色固定不給選，管理員只顯示登入不給註冊切換)→`#hlStepApiKey`(原本兩顆按鈕原封不動搬過來)，都有「← 返回身分選擇」連結
- 新增`splashSelectIdentity()`/`splashBackToIdentity()`/`renderSplashAuthForm()`/`doSplashLogin()`/`doSplashRegister()`，登入註冊成功後呼叫既有的`updateAccountUI()`更新header狀態，再進入API Key選擇
- 新增`superadminBtn`(header，僅`currentUser.role==='superadmin'`時顯示)+`superadminModal`/`adminCreateModal`/`adminEditModal`三個彈窗(比照`classHistoryModal`的結構)：帳號列表(可搜尋)+➕建立帳號(角色含superadmin)+✏️編輯(改顯示名稱/角色/重設密碼)+🗑️刪除
- `usageGuideModal`補一段管理員身分說明

### 後端(`worker/src/index.js`)
新增`requireSuperadmin()`+4個路由：
- `GET /api/admin/users?q=`：列表+關鍵字搜尋(username/display_name)
- `POST /api/admin/users`：管理員專用建立管道，跟自助註冊(`handleAuthRegister`)不同的是**role可以是superadmin**
- `PATCH /api/admin/users/:id`：可選擇性更新displayName/role/newPassword(重新PBKDF2雜湊)
- `DELETE /api/admin/users/:id`：禁止刪除自己目前登入的帳號；刪老師會連帶刪班級+該班歷史記錄(其他學生class_id設回NULL)；刪家長/學生會連帶清`parent_child_links`裡對應記錄

不需要新增migration(role欄位本來就允許superadmin)。CORS的`Access-Control-Allow-Methods`補上`PATCH`。

### 建立第一個superadmin帳號
Steve提供的帳號`iamblue0711`**其實已經是正式站既有的家長帳號**(id=6,顯示名稱「大嬸」)，不是新帳號——直接`UPDATE users SET role='superadmin'`升級，沒有動密碼(沿用原密碼，經curl login驗證登入正常)。

### 驗證狀態(2026-07-28,已用Playwright實機測過正式站,全部通過)
- worker端curl測試：list/create/PATCH(改角色+重設密碼)/自刪擋下(400)/delete(含老師連帶刪班級,已驗證cascade正確)，全部正確；已`wrangler deploy`部署
- 前端Playwright實測`https://lunquiz.pages.dev`：
  - 進場頁5按鈕排版正常(快速使用大按鈕+2x2身分卡片grid)，Phosphor icon正確顯示
  - 管理員登入(`iamblue0711`)：只顯示登入表單無註冊切換，登入成功進到API Key選擇，header正確顯示「大嬸（超級管理員）」+「🛡️管理後台」按鈕
  - 管理後台：列表/搜尋(輸入關鍵字即時過濾)/➕建立帳號(老師角色會出現班級名稱欄位)/✏️編輯(改角色+重設密碼，儲存後清單即時刷新，curl驗證新密碼可登入)/🗑️刪除(confirm對話框文字正確，刪除老師帳號會連帶刪班級，已用curl建立教師帳號直接cascade delete驗證class_count歸零)
  - 老師從新進場頁「老師」身分卡片走「還沒有帳號？點此註冊」完整註冊(角色鎖定不用選、班級名稱欄位正常)，成功後自動進到API Key步驟，選「直接進入」後header正確顯示「XXX（老師）」已登入狀態
  - **踩到一個測試流程細節(非bug)**：後台編輯功能把角色從老師改成家長後才刪除該帳號，因為`DELETE`的cascade判斷用的是**刪除當下**的角色，所以沒有連帶刪班級(設計上合理，只是測試順序要注意)；事後用另一組乾淨的老師帳號直接cascade delete才完整驗證到class一併刪除的路徑
- 已清除全部驗證用測試帳號/班級，正式站D1恢復乾淨(只留`iamblue0711`這個真實升級的superadmin帳號)

### 卡關點
- 無(2026-07-28)

## ✅ header登入按鈕移除+難度分級重做(2026-07-28,Steve截圖回報+提出困難度邏輯問題)

### 1. header「登入/註冊」按鈕整個移除
Steve確認：進場頁已有身分選擇+登入流程，header常駐的「👤 登入/註冊」變成多餘入口。改法：`accountBtn`預設`hidden`，`updateAccountUI()`改成只在`window.currentUser`存在時才顯示(此時文字固定是「👤 我的帳號」，不再有「登入/註冊」文字狀態)。選「快速使用」的訪客現在header完全看不到帳號相關按鈕，符合「登入入口收斂到進場頁」的設計。

### 2. 出題難度分級重新設計
**根因診斷**：舊版難度(簡單/適中/困難)只是原封不動塞進AI prompt的一行文字(「難度：困難」)，AI完全自己猜「困難的國小一年級題目」該長怎樣，容易把知識點拉高到超出該年級課綱(而不是在同年級內加深認知複雜度)。

**Steve選擇的方向**：分級數量從3級增加為5級，且不要用「霸主」這種用詞。

**實作**：
- `<select id="difficulty">`選項改成：基礎／普通／進階／挑戰／精熟(5級)，保留✍️自定義
- 新增`getDifficultyRubric(diffVal)`函式：只對5個預設分級提供明確認知層次定義(基礎=記憶辨識、普通=基本理解應用、進階=應用初步分析、挑戰=多步驟推理、精熟=整合靈活運用)，並在每一級都重申「知識點不可超出該年級課綱，難度只反映認知複雜度層次」；使用者自訂難度文字則不覆蓋，照舊尊重原樣
- 兩個出題入口都加上這段rubric：`startGen()`主要出題流程 + `buildRegenPrompt()`單題重新出，確保單題重出的難度定義跟整份出題一致
- 已用Node `new Function()`驗證script語法、`<style>`大括號配對(122:122)

### 驗證狀態(2026-07-28,已用Playwright實機測正式站)
- 訪客(快速使用)狀態：header完全看不到任何帳號相關按鈕(只剩使用說明/出題紀錄/管理者解除限制)，確認符合預期
- 登入狀態(用superadmin帳號測試)：header正確顯示「大嬸（超級管理員）」+「👤我的帳號」+「🛡️管理後台」
- 難度下拉選單正確顯示5個新選項：基礎/普通/進階/挑戰/精熟(+✍️自定義)
- **還沒驗證**：實際出題比較「基礎」vs「精熟」兩種難度出來的題目內容，確認精熟沒有超出年級課綱、且認知複雜度確實有明顯差異(這需要真的呼叫Gemini出題比對，之後Steve實際使用時可以留意)

### 卡關點
- 無(2026-07-28)

## ✅ 文字微調：「儲存出題條件」「AI智慧出題」(2026-07-28)
Steve截圖指出兩處文字：「儲存設定條件」改「儲存出題條件」、「AI 智慧搜尋」改「AI 智慧出題」，含使用說明彈窗裡對應的文字同步更新。已commit+push部署。

## ✅ 跟Gemini討論後的圖片/插畫策略 + UI/UX全面優化(2026-07-28，Plan模式規劃後實作)

Steve要求跟Gemini討論怎麼加圖片、UI/UX怎麼優化。用Playwright截圖(進場頁/表單頁/結果頁)交給Gemini分析，Gemini指出最大問題是**視覺風格衝突**：表單頁banner是外部hotlink的日系動漫圖(`https://i.urusai.cc/QnrVb.webp`)，跟其他頁面Google四色簡約風完全不搭。Steve確認「全部處理」，進Plan模式後執行：

### 1. 生成品牌吉祥物插畫(取代動漫banner)
用`tools/gemini-auto/gemini_image.py`生成一隻戴學士帽的AI機器人吉祥物(Google四色配色、扁平向量風、正在批改/生成考卷情境)，取代原本hotlink的動漫banner。**踩到的坑**：`gemini_image.py`偶爾會不穩定(3次嘗試中有2次失敗：一次等圖片逾時、一次找不到輸入框)，重試第3次(不加`--new-chat`)才成功；純文字對話(`gemini_chat.py --ask`)當下測試完全正常，判斷是圖片生成流程本身偶發的自動化flakiness，非帳號/登入問題。

### 2. 進場頁4張身分卡片插畫
延續同一隻吉祥物角色，生成學生(抱書)/家長(擁抱小機器人+愛心)/老師(拿教鞭指白板)/管理員(拿盾牌)四個變化版本，取代原本的Phosphor Duotone icon(`ph-student`等)。**踩到的坑**：學生版第一次生成書本封面上被AI加了「知識」「2024」文字，跟「完全不要文字」的需求違背，重新生成一次(明確要求書封面純色無文字)才過關。

### 3. 圖片壓縮
5張圖用Python PIL處理：banner縮到1200px寬、4張角色圖縮到256x256，全部轉存WebP(quality 85-90)，總大小從原始PNG合計約3MB壓到約80KB，存在`public/assets/illustrations/`。

### 4. 出題等待Loading動畫
用Playwright實際瀏覽LottieFiles(WebFetch被LottieFiles擋403，改用Playwright繞過)，在網路請求中找到一個免費loading動畫的直接CDN網址(`https://assets-v2.lottiefiles.com/a/35dde376-1178-11ee-9ed9-5fa2debad506/1LggE3RFrL.lottie`)，色彩剛好是深藍/綠/淺藍/黃橘四顆圓點，完美對應品牌四色。`<head>`加入`@lottiefiles/dotlottie-wc`的CDN script(`<script type="module" src="https://unpkg.com/@lottiefiles/dotlottie-wc@latest/dist/dotlottie-wc.js">`)，出題表單新增`#loadingAnim`(`<dotlottie-wc>`)，在`startGen()`開始出題時顯示、失敗時隱藏(成功時因整個`#inputForm`會被結果頁蓋掉，不需額外處理)。

### 5. 表單4欄→2欄
題數/題型/難度/語言那排`grid grid-cols-1 md:grid-cols-4`改成`md:grid-cols-2`，避免4個下拉選單擠成一排。

### 6. 結果頁：hover才顯示的題目工具列
`regenBtnHTML()`回傳的「🔁重新出這題」按鈕原本常駐可見(黃底)，破壞試卷「像真考卷」的預覽感。新增CSS`.q-item .q-regen-toolbar{opacity:0;transition}` + `.q-item:hover .q-regen-toolbar{opacity:1}`，按鈕div加上`q-regen-toolbar`class，改成滑鼠移到該題才淡入顯示。純CSS改動，不影響`no-print`列印邏輯、不影響`contenteditable`編輯。

### 7. sticky header毛玻璃質感
`.sticky-header`背景改半透明+加`backdrop-filter:blur(12px)`，維持原本深色底+四色細線簽名視覺不變。

### 不做的部分(v1 scope cuts，已在Plan中列出)
- Gemini建議把懸浮工具列從sticky-top改成floating置底圓角膠囊：目前是滿版sticky-top header(不是Gemini截圖誤判的置底浮動列)，改floating pill是更大的版面重構，這次只做玻璃質感不改變位置/形狀
- 空狀態插畫(出題紀錄沒有資料時)：非本次核心痛點，先不做

### 驗證狀態(2026-07-28,已用Playwright實機測正式站，全部通過)
- 進場頁4張身分卡片插畫正確顯示，風格跟banner一致
- 表單banner正確顯示新吉祥物插畫(取代動漫圖)，2欄排版正常(年級/科目、版本/範圍、題數/題型、難度/輸出語言)
- 出題完成後sticky header毛玻璃效果肉眼可見(半透明深色+blur)
- 結果頁hover工具列：用`getComputedStyle(...).opacity`實測確認，滑鼠hover哪一題就只有那一題的「🔁重新出這題」opacity變1、其餘維持0，符合預期
- `customElements.get('dotlottie-wc')`回傳`true`，確認CDN script正確載入且custom element有註冊；console無錯誤(0 errors)
- **還沒驗證**：出題「進行中」那幾秒的loading動畫視覺效果(這次測試出題速度太快沒能截到動畫畫面本身，但已確認dotlottie-wc元件本身有載入成功，之後可另外用較大題數測試看效果)

### 卡關點
- 無(2026-07-28)

---

## ✅ 撤掉AI生成吉祥物，改走ClassDojo式身分徽章 + 產品畫面banner(2026-07-28 晚)

Steve 回報前一版剛加的插畫「很醜」，要求先搜尋同類網站找風格參考再改。

### 參考網站調查結論
| 網站 | 做法 |
|---|---|
| MagicSchool.ai | 紫/青漸層 + **產品截圖為主**，插畫只當配角，機器人是抽象程式方塊造型不擬人 |
| Wayground(原Quizizz，已改名) | **完全放棄吉祥物**，hero是AI漸層 + 產品畫面 |
| ClassDojo | 有強吉祥物系統(Dojo小怪獸)，**但首頁5個身分入口用的是扁平圖示徽章不是角色插畫**，怪獸只活在學生端產品內 |
| Seesaw | 真實照片路線，四個身分入口等權重並列，每身分配對應產品畫面 |
| 均一教育平台 | 吉祥物「狐狸貓」貫穿全站——但那是**給學生**的遊戲化平台，跟本專案定位不同 |

**Steve 補充的關鍵資訊**：lunquiz 最後會改名，變成**學生/家長/老師互動的平台**。所以參考對象從 MagicSchool(純老師端工具)改成 ClassDojo/Seesaw(三邊平台)，且身分入口是**永久的主要導覽**，必須一眼分得出來。

**原本那版為什麼醜(診斷)**：①模仿 Google 官方插畫語彙但比例/透視對不上，明顯AI仿品 ②Steve是Google Partner，四色機器人反而有品牌混淆風險 ③4張身分卡是同一隻機器人換道具，完全沒區分身分 ④白底烤進webp沒去背。

### 實作(Steve選定ClassDojo路線)
1. **4張身分卡插畫 → 扁平圖示徽章**：`.hl-identity-btn` 加 `role-student/parent/teacher/admin` class，各自定義 `--role`/`--role-soft` 兩個CSS變數，chip底色、圖示色、hover邊框、hover反白全部吃同一組變數（之後加身分只要多一條 `role-*` 規則）。圖示用已載入的Phosphor duotone：`ph-student`(藍)、`ph-users-three`(綠)、`ph-chalkboard-teacher`(黃)、`ph-gear-six`(紅)。
2. **表單頁banner吉祥物 → 純CSS考卷示意稿**：`.lq-hero-visual` 兩欄(左文案右考卷)，右邊用CSS拼出微傾斜的A4考卷卡片(標題列+一題選擇題+三條灰線)，不是圖片，改文案/配色不用重生圖。手機版(≤640px)自動收成單欄。
3. **刪除 `public/assets/illustrations/`** 整個資料夾(5張webp已無任何引用；需要時可從git歷史取回)。

### 延後決定
吉祥物**不是不做，是延後**到平台改版、新名稱定案後，再整套設計(多姿態、去背、四身分各自造型、放棄Google四色改自訂品牌色)。現在不把品牌投資綁在會被換掉的名字上。

### 驗證狀態(2026-07-28，已用Playwright實機截圖驗證)
**驗證方式**：`python -m http.server 8899` 起本機靜態站 + 直接寫 Python Playwright 腳本截圖（`playwright` 套件早就裝在 `C:\Python313`，同 `tools/gemini-auto` 用的那套）。腳本留在 `verify_shots.py`，用法 `python verify_shots.py <輸出資料夾> <網址>`，之後改版可重跑。

- 進場頁4個身分徽章：顏色與圖示互相區分明顯（學生藍/家長綠/老師黃/管理員紅），通過
- hover：滑到「老師」時該卡邊框變黃、chip 反白成黃底白圖示，其餘不受影響，通過
- 表單頁考卷示意稿：桌機2欄（左文案右傾斜考卷）、手機390px自動收單欄，皆正常
- **中途修正**：老師原本用 `ph-chalkboard-teacher`，在 48px chip 裡糊成一個方塊看不出是什麼。做了候選圖示比較圖後改成 `ph-presentation`（畫架+板），小尺寸辨識度明顯較好
- **深色模式不用測**：brand.css 的深色樣式是 `.dark` / `[data-theme="dark"]` class 觸發，但 lunquiz 的 index.html 完全沒有主題切換按鈕、也沒有任何地方加這個 class，所以這頁實際上沒有深色模式

### 卡關點
- 無

---

## 📌 快速銜接（2026-07-28 更新，供其他電腦的 Claude 接手用）

### 已完成
- 前端 `public/index.html`（單檔）+ 後端 `worker/`（Cloudflare Worker `linquiz-api`，KV + D1）皆已上線運作
- 正式站 https://lunquiz.pages.dev/ ，API https://linquiz-api.steve-edu711.workers.dev
- 最新一次改版（commit `abbdebb`，subtree push `9fe5a84..33d0610`）：撤掉 AI 生成吉祥物，改成 ClassDojo 式身分徽章 + 純 CSS 考卷示意稿 banner，已用 Playwright 對正式站截圖驗證通過
- 新增 `verify_shots.py`：Playwright 截圖驗證腳本，用法 `python verify_shots.py <輸出資料夾> <網址>`

### 目前狀態
- 功能面穩定，本輪只動視覺
- **發布流程**：在 ClaudeOnly 根目錄 commit → `git subtree push --prefix=school/lunquiz lunquiz master`（remote `lunquiz` → `steveedu711-oss/lunquiz.git`，2026-07-28 才在這台補加）→ Cloudflare Pages 自動部署，約 60 秒生效
- push 前要 `gh auth switch -u steveedu711-oss`
- 這頁**沒有深色模式**：brand.css 有 `.dark` 樣式但 index.html 沒有任何切換機制

### 下一步
- **產品定位要轉向**：Steve 表示 lunquiz 最後會改名，變成學生／家長／老師三方互動的平台（不只是出題工具）。命名討論進行中
- 吉祥物延後到改名定案後再整套設計（多姿態、去背、四身分各自造型、放棄 Google 四色改自訂品牌色）
- 未驗證項目沿用前段記錄：出題「進行中」的 loading 動畫視覺效果尚未實際截到

### 卡關點
- 無

---

## 📌 2026-07-29 修正：PDF 匯出 + 「直接線上測驗」身分分流

### 已完成
1. **PDF 題號後多一整行空白** — `dlPDF()` 原本把 `.no-print` 設成 `visibility:hidden`，元素照樣佔位，每題那顆「🔁 重新出這題」工具列就在題號後留下一行空白。改成 `display:none`（結束時還原原本的 display 值）
2. **PDF 分頁把題目切成兩半** — 原本照 297mm 硬切。改成先量出每個 `.q-item` 底部在截圖裡的像素位置當可切點，切在「最靠近頁尾、又不會切斷題目」的邊界；只接受落在該頁後 60% 的邊界，避免遇到超長題目把整頁切到只剩一小截；找不到合適邊界才退回硬切
3. **「直接線上測驗」改成先選身分**（新 modal `#quizEntryModal`）
   - 🎒 學生 → 沿用 `openAnswerModeNewTab()`，直接開新分頁作答
   - 👨‍👩‍👧 家長 → 需登入家長帳號 → 列出已連結孩子 → 點一位開 `openProctorDetailParent()` 監考式監看
   - 🧑‍🏫 老師 → 需登入老師/superadmin 帳號 → `getOrCreateShareId()` 後新分頁開 `?live=<shareId>` 全班即時進度儀表板
   - 未登入／身分不符會顯示說明＋直接跳登入，而不是把家長老師丟進作答畫面
4. **老師看儀表板不用再輸管理者密碼** — worker `handleLiveProgressList()` 改成「管理者密碼 或 老師/superadmin 的 X-Session-Token」二擇一；前端有 session token 就不再 prompt，401 時才退回問密碼（`window.__liveNeedAdminPass`）

### 目前狀態
- 前端已改完（`public/index.html`），語法檢查通過
- **worker 尚未部署**：`worker/src/index.js` 的授權放寬要跑 `wrangler deploy` 才生效。未部署前，老師身分入口開出的儀表板仍會要管理者密碼（功能不會壞，只是多一步）
- 三項都還沒實機驗證，等 Steve 測

### 下一步
- 部署 worker：在 `worker/` 跑 `npx wrangler deploy`
- Steve 實測：下載 PDF 看題號後空白是否消失、題目是否還會被切斷；點「直接線上測驗」走三種身分

### 卡關點
- 無

### 追加（同日）：免登入化 + 停止監看開關

Steve 要求「這些功能不一定全都要登入帳號」，改成三種身分都能免登入使用，帳號降級成加值選項。

**權限模型**：沿用既有 capability-URL 哲學（誰有連結誰能看），但把「監看權」跟「作答權」拆成兩把不同的鑰匙。

| 身分 | 免登入怎麼進 | 憑證 |
|---|---|---|
| 學生 | 本來就免登入 | 作答連結 `?share=<id>&mode=student` |
| 家長 | 貼上孩子作答頁產生的監看連結 | `?live=<shareId>&student=<studentId>` |
| 老師 | 出題後點「直接線上測驗 → 我是老師」，或「分享 ▾ → 即時進度儀表板」 | `?live=<shareId>&k=<dashKey>` |

**關鍵設計**：老師的儀表板不能只憑 shareId 授權——shareId 跟學生作答連結是同一組，學生自己改網址就能看到全班。所以 `/api/share` 建立時多發一把 `dashKey`（16 碼隨機），存獨立 KV `dashkey:<shareId>`（不放進 `share:` payload，否則公開的 `/api/share/:id` 會把它吐給學生）。老師端連結帶 `?k=`，輪詢時走 `X-Dash-Key` header。

**worker 改動**（`worker/src/index.js`，**尚未部署**）
- `handleShareCreate`：多產生 dashKey，回傳 `{id, dashKey}`
- `hasValidDashKey()`：驗 `?k=` 或 `X-Dash-Key`
- `handleLiveProgressList`：授權改成「金鑰 或 老師/superadmin session token 或 管理者密碼」三擇一
- `handleLiveProgressGet`：學生撤銷後回 403，但持金鑰的老師仍看得到（監考不受學生控制）
- 新端點 `POST /api/live-revoke`：`{shareId, studentId?, revoked}`。帶 studentId＝學生停自己發給家長的連結（憑自己的 shareId+studentId，不用帳號）；不帶＝老師停整份的免登入儀表板（要 dashKey）。可再打一次 `revoked:false` 恢復
- CORS 允許 `X-Dash-Key`

**前端改動**（`public/index.html`）
- `getOrCreateShareId()` 記住 dashKey；`liveDashboardUrl()` 統一組出帶 `?k=` 的連結
- `openAnswerModeNewTab()` 改走 `getOrCreateShareId()`，跟儀表板共用同一 shareId（老師才看得到自己測試的那份）
- 家長免登入視角 `renderLiveParentView()` 從「只有進度%」升級成進度% + 監考式每題作答（資料本來就一起回傳，題目再向公開的 `/api/share` 拿）
- 身分選擇 modal 家長分支：方式一貼連結（免登入）、方式二登入帳號
- 停止監看：學生作答頁 `toggleStopParentWatch()`、老師儀表板 `toggleRevokeDashboard()`，兩邊都是可逆開關

### 部署狀態（2026-07-29 全部完成）
- **Worker 已上線**：`linquiz-api` 版本 `1c42bc20-7041-4d4f-af3e-ec4b27ccd60c`。curl 實測八項全過：回傳 dashKey ✅／公開端點不外洩 dashKey ✅／無金鑰與錯金鑰 401 ✅／正確金鑰 header 與 `?k=` 皆 200 ✅／老師停止監看後 403 ✅／學生停止後家長 403 但持金鑰老師仍 200 ✅／恢復後 200 ✅
- **前端已發布**：subtree push `33d0610..fbbd0cb`，Cloudflare Pages 部署完成，線上 https://lunquiz.pages.dev/ 已含本次程式

### 部署踩到的兩個坑（下次直接照做）
1. **Cloudflare**：環境變數 `CLOUDFLARE_API_TOKEN` 屬於**另一個** Cloudflare 帳號，永遠部署不了 lunquiz（帳號是 steve.edu711@gmail.com）。解法跟 hunglun-new 計數器同一招——指令前綴清掉它走 OAuth：`CLOUDFLARE_API_TOKEN= npx wrangler deploy`（Claude Code 的 `!` 跑的是 bash，不能用 PowerShell 的 `$env:X=''` 寫法）
2. **GitHub 發布 push 403**：system 層 gitconfig 的 `credential.helper=manager`（Windows 憑證管理員）排在 gh 之前，回傳的是 safezone0619 憑證；而且 `gh auth switch` 在**另一次**指令呼叫後就失效。可行寫法是在**同一條指令內**切帳號再推：
   `gh auth switch -u steveedu711-oss && git -c credential.helper= -c credential.helper='!gh auth git-credential' push lunquiz $(git subtree split --prefix=school/lunquiz HEAD | tail -1):refs/heads/master`

### 修正（同日）：撤銷權排除學生
Steve 指正「學生不能按停止監看，只有老師／家長／超級管理員可以」——被監考的人不該有權關掉監考。
- 前端拿掉學生作答頁的停止監看按鈕與 `toggleStopParentWatch()`
- 改放在監看端：老師儀表板（整份）與老師／家長的監考詳情視窗（單一學生），共用 `toggleRevokeStudentWatch()`
- worker `handleLiveRevoke` 改成一律要求憑證之一：儀表板金鑰（老師，免登入）／老師或 superadmin 的 session token／家長 session token 且該 studentId 確實屬於已連結的孩子（新增 `parentOwnsLiveStudent()`，靠 `student-active:<username>` 指標反查，因為即時進度的 studentId 是作答分頁隨機 ID 不是帳號）
- 學生就算自己直接打 API 也停不了（只有 shareId+studentId 不構成撤銷權）
- 免登入家長（純連結）同樣不能撤銷——技術上無法與學生區分，頁面已加提示：要停用請登入家長帳號或請老師處理
- worker 版本 `f6b0648e-f2b2-48c6-99f0-fdba91a316e9`，curl 實測：學生停單人 403 ✅／學生停整份 403 ✅／學生沒停成功家長仍看得到 ✅／老師持金鑰停單人 ok 且家長轉 403 ✅／老師停整份 ok ✅

### 再修（同日）：PDF 仍切到題目的真正原因
Steve 回報改完後仍切到，附上實際 PDF。把 PDF 內嵌的 4 張 JPEG 抽出來看，確認第 1 頁影像**底部本身**就切在第 5 題的 (D) 選項中間，第 2 頁頂端接續同一題 → 是同一個 `.a4-page` 被切片邏輯切開，切點沒落在題目邊界。

**根因**：切點用 `getBoundingClientRect()` 量，那是**視覺**座標（會被瀏覽器縮放、CSS transform 乘上比例）；但 `canvas.height / scrollHeight` 是 **layout** 座標。兩者基準不同，縮放時切點整條線性偏移。Steve 的視窗寬換算是 794px（已套用 <820px 的手機樣式，`min-height:auto` 讓每頁高度隨內容變動、更容易超過 297mm 而進入切片），加上瀏覽器縮放，偏移就顯現。

**修法**：全部改用 layout 座標——新增 `offsetTopWithin()` 累加 `offsetTop` 到 `.a4-page`，配 `canvas.height / pageEl.offsetHeight`，與 canvas 同一基準，不受縮放影響。

**Playwright 實測**（本機 794px 視窗、30 題含長題，跑真的 html2canvas 比對新舊算法）：
| 情境 | 舊算法 | 新算法 |
|---|---|---|
| 無縮放 | ✅ 對齊題目底部 | ✅ 對齊 |
| 縮放 90% | ❌ 偏 19px 切在題目中間 | ✅ 對齊 |

**順手修的兩項**
- 選項標號重複「(A) (A) 14」：AI 重出的題目選項自帶標號，前端又加一次。新增 `stripOptLabel()`，只有開頭字母與該選項標號相同、且後面接括號/句點類分隔符時才移除（不會誤刪「A 原子的質子數」這種正常內容）。試卷、解析卷、線上作答三處渲染都套用
- 出題中的 Lottie loading 動畫 60px → 160px（Steve 反映太小看不到）

### 待辦
- 實機測試（無法自動化，須 Steve 手動）：PDF 下載看題號後空白是否消失、題目是否還被切斷；老師儀表板免密碼、家長貼連結看到每題作答、停止/恢復監看兩邊各一次

---

## 📌 快速銜接（2026-07-29 下午更新，本輪工作結束狀態）

## 已完成
本輪（2026-07-29）全部已部署上線，前後端皆已驗證：
1. **PDF 匯出**：題號後多一行空白（`.no-print` 改 `display:none`）、分頁切斷題目（切點改用 layout 座標 `offsetTopWithin()`，不受瀏覽器縮放影響；Playwright 實測縮放 90% 時舊算法偏 19px、新算法對齊）
2. **「直接線上測驗」身分分流**：學生直接作答／家長貼監看連結或登入帳號選孩子／老師開全班儀表板，三種身分都免註冊
3. **免登入監看權限模型**：老師用獨立 `dashKey`（`?k=`，存 KV `dashkey:<shareId>`，不放進公開端點的 payload）；家長用學生產生的監看連結，並升級成看得到每題實際作答
4. **停止監看開關**：可逆。限老師（金鑰或帳號）／已連結家長（帳號＋親子驗證）／superadmin；**學生不能停**，直接打 API 也回 403
5. **選項標號去重**：`stripOptLabel()` 修「(A) (A) 14」
6. **難度「精熟」→「熟練」**、出題按鈕運算中直接亮 Google 四色跑馬燈（`.is-working`）、Lottie loading 60px→160px

## 目前狀態
- Worker：`linquiz-api` 版本 `f6b0648e-f2b2-48c6-99f0-fdba91a316e9`，八項授權行為 curl 實測全過
- 前端：發布至 `c16d241`，Cloudflare Pages 已部署，https://lunquiz.pages.dev/ 線上確認
- 備份：ClaudeOnly `ce5d48a`

## 下一步
- 等 Steve 實機回報：PDF 重新下載確認不再切題（**要先重新整理頁面再出題**，舊 PDF 是舊程式產生的）
- 產品定位轉向與改名討論仍未定案（見前面段落），吉祥物延後到改名後

## 卡關點
- 無。兩個部署認證坑已解並寫進本檔上方「部署踩到的兩個坑」段落與 Claude 記憶


---

## 📌 2026-07-29 傍晚：匯出格式整頓

## 已完成
- **PDF 頁碼錯亂**：頁尾「第X頁/共Y頁」原本是網頁上的 DOM 分頁(每10題一頁)截圖進去的，
  但 PDF 是照實際內容高度切，兩者頁數不同（Steve 遇到 2 頁的 PDF 卻寫「第1頁/共1頁」）。
  改成截圖時連 `.page-footer` 一起隱藏，全部頁面產生完後用 `pdf.setPage()` + `pdf.text()`
  統一補上「- p / total -」（純數字，避免 jsPDF 內建字型不支援中文變亂碼）。
  pypdf 實測 4 頁分別是 1/4 2/4 3/4 4/4
- **Word 改成真正的 .docx**：舊版是把 HTML 存成 .doc，Word 打得開但 Google 文件/手機 Word 匯入會跑版。
  改用 `docx@9.7.1` UMD（**CDN 必須用 unpkg**：jsdelivr 對 `.cjs` 回傳 `application/node`，
  瀏覽器會拒絕執行）。整份改用 Paragraph/TextRun 重建：標題、班級姓名座號欄、
  題號首行凸排、選項縮排、非選擇題三行虛線作答區、解析卷另起新頁。
  python-docx 實測：20 題、選項標號去重正確、頁邊界 1.5/1.8cm
- **移除純文字 (.txt) 下載**：選單項目與 `dlTxt()` 函式都拿掉，下載選單現在只有 Word (.docx) 與 PDF

## 目前狀態
- 前端已發布上線；worker 這輪沒動
- 匯出三格式縮成兩種：.docx（真 OOXML）與 .pdf（含 10mm 白邊、題目邊界切頁、正確頁碼）

## 下一步
- 等 Steve 實測 .docx 在 Word/Google 文件開啟是否正常、PDF 頁碼與分頁是否正確

## 卡關點
- 無

---

## 📌 2026-07-29 晚間：依身分分流的首頁（大改版）

## 已完成
Steve 指出「學生進入的出題介面應該和家長老師不一樣」，原本所有身分登入後都掉進同一個出題表單（含 API Key、年級/版本/範圍/難度），對學生完全沒意義。改成登入後依 role 進不同首頁。

**Steve 的決策**（AskUserQuestion 確認）
1. 學生可以自己出題練習，用內建額度、不限次數，但沿用 30 秒冷卻；已登入用帳號資料，未登入就填學校/年級/班級/姓名
2. 測驗入口：分享連結與 6 位數代碼**都要**
3. 家長要**完整**出題工具（不是簡化版）

**身分分流**（`enterMainApp()`）
| 身分 | 進入畫面 |
|---|---|
| 學生（登入或免登入） | `#studentHome` 學生首頁 |
| 家長 | 出題工具 + 上方 `#parentPanel` 孩子面板 |
| 老師／管理員／訪客 | 出題工具（原樣不動） |

**學生首頁三塊**：🎯 進入老師的測驗（6 位數代碼／貼連結）、📝 我要練習（年級/科目/範圍/題數/難度，出完題直接進線上作答並自動填好姓名班級）、📊 我的練習紀錄（localStorage，最多 30 筆）

**免登入學生**：進場頁選「學生」→「📝 我要練習（不用註冊）」→ 填學校/年級/班級/姓名 → 存 localStorage `lun_student_profile` → 下次重新整理直接回學生首頁

**測驗代碼**（worker 新端點）
- `POST /api/code` {shareId} → 6 位數代碼（需登入；同一份測驗重複產生會沿用同一組，老師才不會每按一次換一組）
- `GET /api/code/:code` → shareId（不需登入，學生本來就是靠它進來）
- KV：`code:<6碼>` → shareId、`share-code:<shareId>` → 代碼，TTL 同分享 30 天
- 前端：分享選單多一項「測驗代碼（報給學生）」，跳出大字視窗可投影，代碼自動複製

**分享權限再收斂**：學生就算登入也不能發分享連結（他是被指派作答的一方），會提示改用下載 PDF/Word

## 目前狀態
- worker 版本 `977e8b7e-3848-444b-b2c2-a24c948d5d33`，代碼端點實測：未登入產生 401／查無代碼 404／格式錯 400
- 前端 Playwright 實測六項全過：學生入口兩條路、免登入資料帶入學生首頁、老師/家長/學生各自看到正確畫面、學生分享被擋、分享選單含測驗代碼

## 下一步
- 等 Steve 實測各身分流程
- 未做：學生練習紀錄只存 localStorage（跨裝置要後端表）；家長端「孩子最近成績」尚未串（目前只有即時監看）

## 卡關點
- 無
