# linquiz（小綸出題小幫手）進度

## 架構
- `public/index.html`：前端（純靜態頁，之後用 Cloudflare Pages 部署）
- `worker/`：後端 API（Cloudflare Worker，wrangler 部署，負責藏 Gemini API Key + 冷卻限制）

原本是單一 Worker 檔案同時回傳 HTML 又當 API Proxy，且 API Key 由使用者自己貼在前端。
這次改成前後端分離：Key 改放 Worker 端環境變數，前端完全看不到。

## 已完成
- 前端**不提供**使用者自填 API Key 的欄位（已移除，經 Steve 確認拿掉這個功能）
- 後端 Worker：
  - Gemini API Key 只存在伺服器端環境變數，前端完全看不到、也無法自帶 Key
  - **支援多組 Key 輪換**：`env.GEMINI_API_KEYS`（逗號分隔多組 Key，secret，不進 git；只設一組時可用舊的 `env.GEMINI_API_KEY`）。每次請求隨機選一組起始 Key，該模型失敗就換下一組 Key，同一模型下所有 Key 都試完才換下一個模型
  - 冷卻機制：以來源 IP 為單位，透過 KV (`COOLDOWN_KV`) 記錄上次出題時間，30 秒內重複「開始出題」會回 429 + 剩餘秒數
  - 只在每次出題的第一個批次（`X-Batch-Index: 0`）檢查冷卻，避免題數多、批次多時卡到自己
  - 管理者密碼 `5407`（`X-Admin-Pass` header）可略過冷卻，密碼可用 `ADMIN_PASSWORD` secret 覆蓋預設值
- 前端新增冷卻倒數 UI（按鈕會顯示「⏳ 冷卻中，請等待 N 秒」並自動解鎖）
- 前端右上角「🔓 管理者解除限制」按鈕，輸入密碼後之後的請求會自動帶上 admin header
- 模型比照 `D:\ClaudeOnly\SteveProject\lunslip` 的作法，改成雙模型容錯：後端固定依序嘗試 `gemini-3.5-flash` → `gemini-3.1-flash-lite`，前者失敗（額度滿/暫時異常）就自動 fallback 到後者，不會整批直接失敗。原本寫死的 `gemini-3-flash-preview` 不是有效模型名稱，已移除。

## ⚠️ 待確認事項
- 2026-07-23 Steve 貼了 7 組 Key（格式都是 `AQ.Ab8...` 開頭），Steve 說是從 Google AI Studio 申請頁面拿到的，但這個格式跟一般 Gemini API Key（通常 `AIzaSy...` 開頭）不太一樣。用第一組做過一次 curl 驗證但因手誤把清單編號也算進去而失敗，之後被系統擋下沒能重測。**這幾組 Key 目前還沒有驗證過是否真的能用**，部署後 Steve 要自己實測確認。

## 目前狀態：尚未部署，需要 Steve 手動做以下事情

### 1. 建立 KV namespace（一次性）
```
cd D:\ClaudeOnly\tools\linquiz\worker
npx wrangler kv namespace create COOLDOWN_KV
```
把輸出的 `id` 填入 `wrangler.toml` 的 `REPLACE_WITH_KV_NAMESPACE_ID`。

### 2. 設定 Secret（一次性）
```
npx wrangler secret put GEMINI_API_KEYS
```
會進入互動模式提示貼入內容（不會顯示在畫面、也不會存進終端機歷史），把 7 組 Key 用「逗號」串成一行貼進去，例如：
```
key1,key2,key3,key4,key5,key6,key7
```
（若想覆蓋預設管理者密碼 5407，可另外 `npx wrangler secret put ADMIN_PASSWORD`）

### 3. 部署 Worker（後端）
```
npx wrangler deploy
```
會得到一個網址，例如 `https://linquiz-api.<你的subdomain>.workers.dev`。

### 4. 更新前端 API_BASE
打開 `public/index.html`，把 `const API_BASE = "https://linquiz-api.YOUR_SUBDOMAIN.workers.dev";`
換成步驟 3 拿到的實際網址。

### 5. 部署前端（Cloudflare Pages）
依照全域部署標準：在 Cloudflare Pages 建立專案，指向這個資料夾，Build command 留空、
Output directory 設為 `public`。之後照「發布」流程走（subtree push 到 steveedu711-oss）。

## 下一步
- Steve 完成上面 1~5 步部署測試
- 部署後實測那 7 組 Key 哪些能用（見上方「⚠️ 待確認事項」）
- 確認冷卻倒數與管理者解鎖行為符合預期後即可正式使用

## 卡關點
- 7 組 Key 格式不像標準 Gemini API Key，尚未驗證有效性，需 Steve 部署後自行測試
